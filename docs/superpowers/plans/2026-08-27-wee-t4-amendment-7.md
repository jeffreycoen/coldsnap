# Task 4 combined — Amendment 7: edit 5's first pattern corrected

Amendment 6's edit 5 wrote the seat-counter pin's first new pattern in an
assignment form the tree never had. boot.js builds the war as one
literal; the line (boot.js:162 region) is `seq: { apc: 0 }` inside the
object. The corrected edit 5, whole — everything else in amendment 6
stands, edits 1–4 are already in the tree:

**09-reorg.mjs line 60** — re-teach, old → new:
old `/let apcSeqN = 0;/` and `/const nextApcSeq = \(\) => \+\+apcSeqN;/`
on the DepotGame read
→ new `/seq: \{ apc: 0 \}/` and
`/const nextApcSeq = \(\) => \+\+war\.seq\.apc;/`
on a boot.js split read. The check's label re-signs from "stays a mount
let" to "lives on the war (task 4)" — the one label change, reported
old→new.

Then the landing exactly as amendments 5 and 6 wrote it.

# DEPOT Phases 0+1 Implementation Plan

*HISTORY (stamped 2026-08-20, owner's order): this document is a record of shipped work, not authority. Design truth lives in CLAUDE.md and the ACTIVE phase's plan documents; where this file disagrees with them, it is wrong.*

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land the shared combat substrate (glancing/armor damage, tree destruction) as guarded core hooks, then scaffold the COLDSNAP DEPOT module — defend side, wave stalls, dispatch cards, 50-wave structure.

**Architecture:** Core changes follow the TD-hook discipline: every new behavior is gated on a world/spec flag that no existing mode sets, so golden and all TD gates stay bit-identical. DEPOT is a new `src/depot/` directory seeded from ColdsnapTD's loop but split into modules from birth.

**Tech Stack:** Existing — core.js + renderer.js + React + Vite. No new dependencies.

## Global Constraints

- HOLD THE DEPOT (`src/game/ColdsnapTD.jsx`) is **not modified** in these phases.
- The frozen demo, sandbox, and campaign are untouched.
- Every CI gate that exists today must pass after every task: `golden`, `test:aar`, `test:deviation`, `test:predicate`, `test:scenario`, `test:righting`, `test:campaign`, `test:td`, `test:td-render`.
- **Seeded rng only in `src/depot/`**: no `Math.random()` in any sim-affecting code. Enforced by a lint gate (Task 1).
- All randomness in new core paths draws from `world.rng` only, and only when the guard flag is set (rng draw count is hash-load-bearing for other modes).
- Anchors below use line numbers from the 2026-08-09 audit; **verify each anchor by reading the code before editing** — drift is possible.
- Commit format: imperative subject, `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>` trailer.

---

## Phase 0 — Substrate

### Task 1: Seeded-rng lint gate

**Files:**
- Create: `scripts/depot-lint.mjs`
- Modify: `package.json` (scripts)

**Interfaces:**
- Produces: `npm run lint:depot` — exits 1 if any file under `src/depot/` contains `Math.random`.

- [ ] **Step 1: Write the gate**

```js
// scripts/depot-lint.mjs
import { readdirSync, readFileSync, statSync } from "node:fs";
import { join } from "node:path";
const bad = [];
function walk(dir) {
  for (const f of readdirSync(dir)) {
    const p = join(dir, f);
    if (statSync(p).isDirectory()) walk(p);
    else if (/\.(js|jsx)$/.test(f) && readFileSync(p, "utf8").includes("Math.random"))
      bad.push(p);
  }
}
try { walk("src/depot"); } catch { /* dir may not exist yet — pass */ }
if (bad.length) { console.error("Math.random forbidden in src/depot:", bad); process.exit(1); }
console.log("depot-lint PASS");
```

- [ ] **Step 2: Add script + run**

`package.json` scripts: `"lint:depot": "node scripts/depot-lint.mjs"`. Run: `npm run lint:depot` → expect `depot-lint PASS`.

- [ ] **Step 3: Commit**

### Task 2: Glancing damage (impact geometry) — guarded core hook

**Files:**
- Modify: `src/engine/core.js` (projectile impact resolution, near the segBoxHit consumption ~:683-700 — verify anchor)
- Create: `scripts/combat-test.mjs`
- Modify: `package.json`, `.github/workflows/deploy.yml` (add `test:combat` gate)

**Interfaces:**
- Consumes: existing `segBoxHit` impact path and `applyDamage(world, b, dmg, info)` (core.js:724).
- Produces: when `world.depotCombat` is truthy, direct projectile damage is scaled by impact obliquity: `dmg * (0.35 + 0.65 * |cos θ|)` where θ is the angle between the projectile velocity and the face normal of the struck AABB face. Face normal = axis of least penetration from segBoxHit (expose it: segBoxHit already computes entry axis internally — return it as `hitAxis`; verify by reading segBoxHit before coding). Flag is set ONLY by DEPOT worlds.

- [ ] **Step 1: Write failing test** — `scripts/combat-test.mjs`: build a minimal world (`world.depotCombat = true`), place a box body, fire one projectile head-on and one at a grazing angle (~75° off-normal) with identical dmg; assert grazing victim retains more hp. Also assert: same scenario WITHOUT the flag → identical damage both cases (guard proof).

- [ ] **Step 2: Run to verify fail** — `node scripts/combat-test.mjs` → head-on and graze deal equal damage.

- [ ] **Step 3: Implement** — thread `hitAxis` out of segBoxHit; in the impact-damage branch compute obliquity from normalized projectile velocity component along hitAxis; scale only under `world.depotCombat`.

- [ ] **Step 4: Verify pass + full gate sweep** — `node scripts/combat-test.mjs` PASS, then `npm run golden && npm run test:td && npm run test:righting` (fast trio) then remaining gates.

- [ ] **Step 5: Commit**

### Task 3: Armor thresholds — guarded core hook

**Files:**
- Modify: `src/engine/core.js` (`applyDamage`, core.js:724)
- Modify: `scripts/combat-test.mjs`

**Interfaces:**
- Produces: bodies may carry `b.armor` (number, default undefined). Under `world.depotCombat`, `applyDamage` with `info.kind` of a ballistic cause and `dmg < b.armor` applies `dmg * 0.15` (a glance-off); `dmg >= b.armor` applies full. Blast damage (`info.cause === "blast"` — verify the actual info shape in explode()'s applyDamage calls before coding) bypasses armor entirely (concussion). No body outside DEPOT ever has `b.armor`.

- [ ] **Step 1: Extend combat-test** — armored body (armor 40): 30-dmg round → 4.5 hp lost; 50-dmg round → 50 lost; 30-dmg blast → 30 lost. Unflagged world: armor ignored.
- [ ] **Step 2: Verify fail** → **Step 3: Implement** (≤10 lines in applyDamage) → **Step 4: combat-test + full sweep PASS** → **Step 5: Commit**

### Task 4: Tree destruction — fire on rounds, MG shredding

**Files:**
- Modify: `src/engine/core.js` (tree handling; blasts already burn trees at ~:596-598 — verify)
- Modify: `src/render/renderer.js` (tree visual states)
- Modify: `scripts/combat-test.mjs`

**Interfaces:**
- Produces (all under `world.depotCombat` OR `world._tdStruct` — decide while reading how TD gates struct hits, keep TD's current visuals unchanged unless flag set):
  - Trees get `t.hp` (default 30). Direct projectile hits: shell/rocket kinds ignite (`t.burning = world.t`), mg kind subtracts `t.hp -= 4` per hit; at `hp <= 0` the tree falls (reuse the existing felled-tree state from the map pass — verify its field name in renderer).
  - Burning trees lose 2 hp/s and render flame/char progression; burnt-out = felled + charred tint.
- [ ] **Step 1: Headless test** — mg rounds fell a tree in ~8 hits; a shell ignites; burning tree dies in ~15s. Unflagged world: trees inert to rounds (TD/campaign unchanged).
- [ ] **Step 2: Verify fail** → **Step 3: Implement core** → **Step 4: Renderer states** (screenshot evidence, both light/heavy gfx) → **Step 5: combat-test + full sweep + `test:td-render` PASS** → **Step 6: Commit**

---

## Phase 1 — DEPOT scaffold

### Task 5: Module scaffold + start-screen entry

**Files:**
- Create: `src/depot/DepotGame.jsx` (loop shell), `src/depot/state.js` (run state: wave idx, phase enum `build|wave|stall`, resources), `src/depot/specs.js` (tower/enemy specs imported/copied from TD values)
- Modify: `src/ui/StartScreen.jsx`, `src/App.jsx` (screen wiring, autosave `coldsnap-depot-*` keys)

**Interfaces:**
- Produces: screen id `"depot"`; `DepotGame({ onExit })`; world built with `world.depotCombat = true` via the scenario loader's proving-grounds-style spec or makeMap (reuse TD's map path — read how ColdsnapTD builds its world and mirror it).
- Seeds: ALL gameplay rng from `world.rng`; aim/cooldown jitter that TD did with `Math.random()` must use `world.rng()` here (lint gate enforces).

- [ ] **Step 1:** Copy TD loop → strip to: map build, camera, build/sell UI, tower fire, wave spawn (flat conscript waves for now), depot leak/lives. Replace every `Math.random()` with `world.rng()`.
- [ ] **Step 2:** Start screen gains COLDSNAP DEPOT; entry/exit works; `npm run lint:depot` PASS.
- [ ] **Step 3:** Build + local smoke by hand (screenshot: depot screen, a wave dying to towers, trees shredding under MG).
- [ ] **Step 4: Commit**

### Task 6: Wave stalls + dispatch shell

**Files:**
- Create: `src/depot/Dispatch.jsx` (teletyped card — reuse the campaign `Typed` component pattern; ACKNOWLEDGE dismiss, armed 500ms per the modal lesson; re-readable from bar)
- Modify: `src/depot/DepotGame.jsx`, `src/depot/state.js`

**Interfaces:**
- Produces: hard stall between waves — sim keeps ticking (idle world) but no spawns until ACKNOWLEDGE; `state.dispatch = { wo, lines[] }`; placeholder bureau copy ("WAVE n CLEARED. HOLD."). This stall is the future multiplayer sync point — structure it as `phase: "stall"` with a single `advance()` call so a network gate can replace the button later.
- [ ] **Step 1:** State machine test in `scripts/depot-test.mjs` (headless: build → wave → stall → advance → wave 2).
- [ ] **Step 2:** Implement + browser check (teletype, ACKNOWLEDGE, 500ms arming).
- [ ] **Step 3: Commit**

### Task 7: 50-wave structure + win/loss shell

**Files:**
- Modify: `src/depot/state.js`, `src/depot/DepotGame.jsx`

**Interfaces:**
- Produces: run ends at depot destroyed (LOSS), wave 50 survived (WIN — economic-power comparison stubbed as `resources` vs a placeholder enemy ledger until Phase 3), or regiment-destroyed hook (stub false). End card (reuse dispatch card style). Wave composition scales linearly for now (real economy is Phase 3).
- [ ] **Step 1:** Headless: force-lose (no towers) → LOSS; god-mode towers to 50 → WIN.
- [ ] **Step 2:** Implement → **Step 3: Commit**

### Task 8: Gates — `test:depot` + smoke section

**Files:**
- Create: `scripts/depot-test.mjs` (consolidate Task 6/7 asserts: determinism double-load same seed → same worldHash after scripted wave; stall machine; win/loss; guard-flag proof — TD world unaffected by depot hooks)
- Modify: `package.json`, `.github/workflows/deploy.yml`, `scripts/smoke.mjs` (section: enter DEPOT, wave 1 clears, dispatch ACKNOWLEDGE, wave 2 spawns; use waitForFunction+polling for ALL DOM asserts per the smoke race lesson)
- [ ] **Step 1:** Wire gates, run full local suite + build + smoke against preview.
- [ ] **Step 2:** Commit, push, verify CI green + prod smoke, screenshots to Jeff.

---

## Self-review notes

- Spec coverage: Phase 0/1 rows of the roadmap fully tasked; accuracy/wind/economy/territory deliberately absent (later phases).
- Anchors are audit-sourced; every task says verify-before-edit.
- `world.depotCombat` is the single guard flag across Tasks 2-5 — consistent.
- Tree felled-state field name unverified — Task 4 Step 3 must read the map-pass code first (flagged in-task).

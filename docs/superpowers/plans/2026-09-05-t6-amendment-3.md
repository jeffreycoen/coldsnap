# The Shell Carved — Task 6, Amendment 3: the discipline capture

The moved `view.setTowerDiscipline` reads the component-local
`discipline` binding. That binding's only writer, `view.setDiscipline`,
sets `discipline` and `input.discipline` together in one line — and is
itself dead code, called from nowhere (the source's own comment beside
it records this as a reported finding). `input.discipline` therefore
carries the identical value on every path, and `input` is already in
ctx.

## The substitution (added to the plan's table)

| moved text | becomes | where |
|---|---|---|
| `(b.discipline \|\| discipline \|\| "careful")` | `(b.discipline \|\| input.discipline \|\| "careful")` | orders.js, `view.setTowerDiscipline` — the one bare `discipline` read in the moved set |

Everything else in the approved plan and Amendments 1–2 stands. Any
further captured local not in ctx stops the task again and is reported
the same way.

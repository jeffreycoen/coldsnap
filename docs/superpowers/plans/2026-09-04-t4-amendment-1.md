# The Shell Carved — Task 4, Amendment 1: the thirty-fifth pin

The task stopped at depot-test with 2,163 PASS / 1 FAIL. The one failure
is a source-text pin the plan's sweep missed; under the recorded ruling
it is deleted like the others. The plan's own count is also corrected:
its table names 34 checks, not the 33 its prose said — all 34 landed.

This amendment governs the already-dispatched task 4 tree (edits and the
0.3.13 bump in place, uncommitted, head b35e30a). Everything in the
approved plan stands except as amended here.

## The deletion

| file | check |
|---|---|
| `scripts/tests/04-vision-command-possession.mjs` (near line 1393) | "POSSESSION T3(d) source pin: the possess wedge only pushes when canPossess" |

One `ok(...)` statement, matched by its quoted name, removed whole. An
orphaned source-read variable goes with it if no surviving check uses
it, reported. Any further failure of any kind stops the task again.

## Amended acceptance

- depot-test **2,163 PASS / 0 FAIL** (2,198 minus the 35 total
  deletions).
- golden 7 PASS; smoke 30 / 0 — unchanged.
- All other acceptance lines of the approved plan stand; the commit
  subject's "thirty-three retired pins" becomes "thirty-five".

## The phase document (at this landing)

The depot-test baseline row → 2,163; T4's status row LANDED with commit
hash and gate counts, the 35-pin total stated.

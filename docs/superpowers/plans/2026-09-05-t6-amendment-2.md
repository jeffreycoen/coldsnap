# The Shell Carved — Task 6, Amendment 2: three more names

The agent's pre-edit verification found three further identifiers the
moved bodies reference that neither the ctx list nor the (amended)
import block carries. All three join orders.js's imports:

- `import { SQUAD_SPECS } from "./squads.js";` — new line
  (`tapAt`'s squad-cost read). SQUAD_SPECS STAYS imported in the
  component (live uses outside the moved block).
- `PALETTE_BY_KEY` joins a `./palette.js` import line — new line
  (`tapAt`'s hire and hero ghost costs). PALETTE_BY_KEY STAYS imported
  in the component (live uses outside the moved block).
- `WALL_FIELD_COST, SANDBAG_FIELD_COST` join the existing `./state.js`
  import line (`refreshLinePreview`, `input.stepChainBuild`). Both are
  zero-use in the component after the move and join the dead-import
  removals there, same rule as `eyeOf`.

Everything else in the approved plan and Amendment 1 stands. Any
further missing name stops the task again and is reported the same way.

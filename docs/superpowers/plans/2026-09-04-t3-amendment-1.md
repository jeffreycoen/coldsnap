# The Shell Carved — Task 3, Amendment 1: the nine pins retire

The task stopped at depot-test with 2,198 PASS / 9 FAIL. All nine
failures are source-text checks — greps over DepotGame.jsx's text that
now miss because the pinned rows moved to the new files. Source-text
checks are not functionality tests. The nine are DELETED, not
re-pointed. The wider ruling is recorded in the phase document: the
source-text-pin convention retires, and a full sweep of the suite
becomes its own task at the end of the phase.

This amendment governs the already-dispatched task 3 tree (edits in
place, uncommitted, head 53d826c). Everything in the approved plan
stands except as amended here.

## The deletions (each is one `ok(...)` statement, removed whole; the
fixture code around it stays)

| # | file : line (live tree) | check name |
|---|---|---|
| 1 | `scripts/tests/03-bell-polish.mjs` : 473 | mk0.60/6: the engineer team is on the build bar |
| 2 | `scripts/tests/11-hiring-hall.mjs` : 553 | T6(h2): the bar, the mode map, and the tend call are wired |
| 3 | `scripts/tests/11-hiring-hall.mjs` : 664 | T7v2(i2): the bar, the mode map, the loop call, and the books stamp are wired |
| 4 | `scripts/tests/11-hiring-hall.mjs` : 723 | T8v2(d): the draft screen exists on its own attributes, confirm arms at five |
| 5 | `scripts/tests/11-hiring-hall.mjs` : 735 | T8v2(d7): picks hold by the card's name — highlight and un-pick both work, duplicates impossible |
| 6 | `scripts/tests/24-the-quartermaster.mjs` : 19 | names: the bar follows the trades |
| 7 | `scripts/tests/24-the-quartermaster.mjs` : 22 | names: the enemy rack follows |
| 8 | `scripts/tests/24-the-quartermaster.mjs` : 61 | lattice: the enemy pair follows |
| 9 | `scripts/tests/25-the-teaching-cards.mjs` : 70 | T6: the wedge opens its card by press or ⓘ |

Line numbers are anchors only; each deletion is matched by the quoted
check name. A deletion that would orphan a variable used by no other
check removes that variable's declaration too, reported. No other check
is touched — a tenth failure of any kind stops the task again.

## Amended acceptance

- depot-test **2,198 PASS / 0 FAIL** (2,207 minus the nine).
- golden 7 PASS; smoke 30 / 0 — unchanged.
- All other acceptance lines of the approved plan stand.

## The phase document (edited at this landing)

1. The ruling, added under Standing constraints: source-text pins —
   suite checks that grep a source file's text rather than run its
   behavior — are retired. No new one is ever written; movers delete the
   ones their moves break; the full sweep is T8.
2. New index row before the closeout:
   `T8 | 0.3.17 | The pin purge: every remaining source-text pin swept
   from the suite; behavior checks stay | OPEN` — and the closeout
   re-numbers to 0.3.18.
3. The depot-test baseline row: 2,207 → 2,198 at this landing.

## Report addition

The nine deletions each named old→gone; the new pass count stated; the
commit subject stays the approved plan's own.

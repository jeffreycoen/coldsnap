# F1.5 / F1.6 Completion Plan — Artillery, Vision, and the Close Gate

---

# PART ONE — THE PLAIN VERSION (Jeff reads this; nothing below the line is required reading)

**What this plan delivers, in order:**

1. **Rockets learn to arc.** The rocket tower finally lobs over hills like it always claimed to. Its aim gets re-tuned so lobbed damage matches today's — measured, not guessed.
2. **Rocket infantry, both sides.** You can buy a two-man rocket team; the enemy fields rocketeers that answer your wall lines. Same weapon numbers both sides, as always.
3. **Fog becomes eyesight.** Today you see wherever you *own* ground. After this, you see what your soldiers and towers *actually see* — a sniper on a ridge lights the valley beyond it even deep in enemy ground, and a valley behind your own lines stays dark until someone looks into it. Ownership (building rights, income, the color wash) doesn't change.
4. **Your guns shoot what your side can see** (your decision, locked): if anyone on your side has eyes on a target, your shooters may engage it.
5. **The spotter earns his hill:** his elevated eyes light more map than anyone else's — the pair's real payoff.

**What must be fixed before this phase closes (your rule):**

- **Sandbags must be visible.** Diagnosis running; you confirm on your device.
- **Sappers must work in your hands.** The doubled-charge fix is being built; you re-run your 20-team assault to confirm.
- **Units must stop looking identical.** A lineup photograph of every unit type is being made; you'll direct the look from it.
- **Two small tap annoyances** around the confirm button get fixed.

Each item closes when *you* confirm it in play — not when a test passes.

**What should feel different when it all lands:** rockets arcing over ridges; enemy rocket fire answering your walls; dark valleys you own and lit ridges you don't; a spotter worth protecting; sandbags you can see; sappers that demolish; an army you can tell apart.

**Decisions already yours and locked:** targeting follows vision; selection keeps one range fan; the stray-tank note is dropped; unit looks await your read of the photograph.

---

# PART TWO — TECHNICAL BRIEF (for the implementing agents; Jeff stops here)

> Opus 5, one implementer per task, in order unless marked independent. Every dispatch: mandatory reading list + trap notes; agent lists "files read before first edit" in its report. Failing asserts FIRST. Scoped verification only. FOREGROUND everything. Max 3 cycles then BLOCKED. Reports plain-language, every deviation its own labeled bullet.

## A — F1.5 remainder (approved `2026-08-11-f1-5-artillery.md` governs)
- **A1 — rocket retune (next free mk):** sweep acc 0.020-0.035 lobbed vs flat baseline 2.4592 ±10%; `towerShot` high for rocket; close the held 6.5 task. Read: specs.js rocket, state.js towerShot, 6.5 plan STATUS curve, depot-test rocket fixtures. Traps: the acc cliff (0.340 lobbed = 0 DPS, measured); volley 4 @ 0.12s preserved.
- **A2 — rocket teams:** INFANTRY_ARMS.rockets at A1's acc (volley 2, fireRate 6.0, cost 40 provisional); enemy rocketeer via spec-parameterized stepGrenadier (byte-parity pin when passed its own spec); ai.js wall-pressure counter-buy with ZERO new draws (4-draw planWave pin). squadFire volley threading: `spec.burst || spec.volley || 1`. Read: squads/state fire paths, units.js stepGrenadier+spawnUnit, ai.js sniperWanted pattern, DepotGame build bar/SQUAD_MODE, depot-test stream pins.
- **A3 — artillery closer:** smoke artillery section; vision-doc note; one summary table of all measured numbers.

## B — F1.6 vision (supersedes the outline doc)
- **B1 — vision.js:** two Uint8 layers (per team) on the territory grid family (cs 2, canonical coords — every eye through invW). eyeFootprint: 48 azimuth rays, 1.2m step, straight sightline eye→(cell ground + 1.2) via solidBlocksPoint with selfId; EYES = { unit 26, tower 34, sniper 40, spotter 48 } provisional. stepVision at the 1Hz territory cadence; per-eye cache keyed (cell-quantized pos, terrain epoch — wire off field.dirty; READ its propagation first); towers compute once. visStateFor: visible / silhouette (one-cell rim adjacency) / hidden. Perf budget < 8ms/rebuild on the Pi, measured and printed. Asserts: crest-vision fixture (rise sees over, valley doesn't), solids occlude, selfId, cache correctness incl. crater bump, rim derivation, both-sign symmetry, zero rng, twin determinism. Read: territory.js (pattern), accuracy.js solidBlocksPoint/reachPolygon (REUSE the point test), DepotGame terrAcc/TERR_STEP + field.dirty path. Traps: canonical-vs-world; fog toggle stays visuals-only.
- **B2 — renderer swap:** fog inputs (sample/sampleUV) read visStateFor; wash/tint (sampleVal) stays control; enemy bodies render iff cell visible, rim = existing silhouette treatment; DEPOT-gated; td-render byte-parity with no option. Proving case: enemy behind a ridge on CONTROLLED ground hidden; enemy visible to a forward sniper renders deep in red ground. One screenshot for Jeff. Traps: 4Hz retint vs 1Hz vision are different clocks — don't merge.
- **B3 — targeting follows vision (LOCKED):** fieldReaches' unit-target fog gate reads visStateFor; structure fire stays ungated (law); every unit-law assert re-pointed both signs; twin determinism.
- **B4 — spotter's eyes (LOCKED: one fan):** EYES.spotter footprint; asserts: spotter > sniper from the same hill; pair lights cells a lone sniper doesn't. Render/selection unchanged otherwise.
- **B5 — closer:** Pi perf numbers recorded; smoke vision section; playtest framing.

## C — F1 Task 5 (independent; runs on Jeff's word)
Probe learns breach endings; scripted siege proof (real post-sapper-fix); two-depot smoke; stale-doc sweep.

## D — CLOSE GATE (each row: fix + Jeff's in-play confirmation)
| Issue | State | Resolution requires |
|---|---|---|
| Sandbags invisible | DIAGNOSED (renderer.js:1433 — chunk draw cap 1000 < 1154 boot chunks since 361dcfc added depot2's masonry; 154 town stones ALSO invisible; pool allocated 1800; screenshots .superpowers/sb-*.png) | Jeff's fix spec (2026-08-11): DOUBLE the cap (1000 → 2000, pool grown to match, one shared constant + in-code note), loud dev warning if ever clipped again, HUD shows "stones drawn / limit" beside the fps readout, smoke gains an instance-count assert (chunk bodies == drawn) + one place-a-sandbag pixel check. Dispatches AFTER the sapper task lands (shared files). → Jeff sees a sandbag on his device. |
| Sapper siege | mk0.21 in flight | Report delivers massed+sequential breach numbers, new --sides table, declared pin moves → Jeff re-runs his 20-team assault. |
| Units look identical | Lineup photo in flight | Photo + per-unit sameness list → Jeff directs the look → identity work joins this gate. |
| ✓-confirm tap swallows | Diagnosed (:950, :1131) | Small fix: unarmed-✓ tap is inert (no silent next-tap cancel); pending auto-cancels when its buttons leave screen. |
| ~~Stray tank~~ | DROPPED (Jeff) | Enemies staying on the field is now by design. |

## Facts that must flow into fix briefs (nothing dispatches without them)
1. From the sandbag diagnosis: the excluding line, causing commit, screenshot proof.
2. From the sapper implementer: both breach numbers, moved pins.
3. From the lineup agent: the photograph + sameness list.
4. From Jeff: playtest confirmations per D row; if sandbags ever "work in tests but not for him" again — his device/browser + the corner version at that moment.

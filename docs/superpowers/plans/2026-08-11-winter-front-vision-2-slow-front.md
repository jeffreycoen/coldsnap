# WINTER FRONT — Vision II: The Slow Front

*2026-08-11 — successor to `2026-08-11-depot-front-vision.md`. That document's law stands: two depots, destroy-to-win, symmetric war. This one layers pace, command, and terrain on top and re-sequences everything that remains. Every decision in here was ratified by Jeff on 2026-08-11; nothing is open.*

*Amended 2026-08-12 off Playtest I (phases C0 + 1 shipped, mk0.43): starting kit gains engineers and seeded sandbags; the economy becomes the mercenary market (§2a); weapon-voice identity added to presentation; Vision phase moves ahead of Command. All amendments Jeff-ratified 2026-08-12.*

---

# PART ONE — The Vision (plain language)

## 1. Premise

The war slows down and the ground starts mattering.

Today the game hands you the full arsenal in the first minute and the enemy floods the field on a timetable. Instead: every match starts small — walls, sandbags, one rifle squad — and the war *arrives*, piece by piece, on a rhythm the whole game shares. You learn each weapon because it came off a truck in front of you, not because you read a menu. The map gets wider, emptier of noise and fuller of terrain that rewards thought: forests, streams, bridges, a dam holding back a lake. Artillery fires half as often and means twice as much. Dead men stain the snow and the stains stay.

One law survives from the first vision untouched: **symmetry**. Anything you can field, build, or blow up, the enemy can too.

## 2. The Bell

The wave counter dies. In its place: the **muster bell** — a shared clock both armies live on. Play never pauses between bells; you build, move squads, patrol, skirmish. When the bell rings, four things happen in order:

1. **Intel** — the report on what the enemy has and where.
2. **Income** — the cycle's scrap arrives.
3. **The manifest** — a supply convoy offers 2 or 3 new items; you pick **one**. That pick is how walls-and-rifles becomes mortars, engineers, rockets, and eventually heroes.
4. **The assault** — the enemy comes, with whatever their own bells have brought them.

The enemy draws from the same tiers on the same bells — their pick can differ from yours, so no two wars arm identically. Every match runs the full arc from nothing to everything; no progress carries between matches. The game auto-saves at each bell so a phone match can be left and resumed — but when your depot falls, the run is over and the save burns. No rewinding a war.

The ladder, roughly: fortification first (engineers, sandbag lines), then infantry variety (MG, snipers), then artillery (mortars **or** rockets — a real choice), then sappers, then heroes.

*(Amended 2026-08-12: the starting kit is walls, sandbags, a rifle squad **and an engineer team** — a minimal one at first, able only to build sandbag lines along a tapped route, so fortification is learned from minute one; the full engineer kit still arrives in its own phase. The depot also starts with a few sandbags already scattered around it — cover exists before the player builds any.)*

Artillery itself slows: mortars and rockets fire **half as often**, both sides. A salvo becomes an event you watch land, not a stream.

## 2a. The Mercenary Market *(added 2026-08-12)*

Both armies hire from **one shared mercenary market**. Every unit and tower type has a reserve — companies available for hire — and prices ride a bonding curve on that reserve (the AMM shape): each purchase drains the pool and raises the price for the *next* buyer, on either side; reserves replenish between bells as new companies reach the front, easing prices back down. Deterministic, no dice.

What this buys, beyond flavor:

- Rising prices finally *mean* something — sourcing troops in a war zone is hard, and gets harder the more everyone hires.
- The market is the performance governor in disguise: the global pool of hireable men and materiel is sized to what the engine can afford to simulate, so the game never shows a cap — just a price.
- Spam has a cost curve. The sixth rifle squad is dear; doctrine variety pays.
- Because the market is shared, it becomes a front of its own: buying out the sappers before the enemy can is a real, legal move — and the law of symmetry holds, since they can do it to you.

Two rules settle how the shared market runs *(ratified 2026-08-12)*: purchases resolve **first-come, first-served in real time** — no turns, no priority; timing is a skill, and the enemy staggers its buying through the cycle rather than shopping in one predictable batch. And territory feeds the market through **supply, not prices**: ground you hold speeds your reserves' replenishment — supply routes, not discounts — symmetric by rule.

## 3. Command

When you place a unit — or tap one — a **radial menu** opens showing everything it can do. It replaces the current chip buttons entirely. Dismiss it and the unit simply defends where it stands; the menu is depth, never a toll.

Soldiers learn new orders: **take cover** (find the best nearby cover facing the threat and dig in), **patrol** a route, **hold fire** (an ambush waiting to happen), **attack structures**, **fall back** (withdraw while shooting), **escort** another squad, **suppress/barrage** a patch of ground, and for sappers, **demolish this** — a directed target.

Towers, which today think for themselves, gain doctrine: target priority (nearest, armor first, infantry first, or *focus what I tap*), hold fire, careful/free fire, a sector limit, and area barrage for the lobbers.

The radial doubles as the teaching surface: when the manifest delivers something new, its radial is where you discover what it does.

One squad fix rides along: a squad now waits only a few seconds for a straggler before advancing — no more whole squads frozen by one man wedged in rubble.

## 3a. Possession *(added 2026-08-12)*

**Anything on your side can be taken over and driven.** TAKE CONTROL is an entry on every friendly radial — squads, towers, and eventually heroes. Possessing a squad makes you its leader: the stick drives the formation and the fire button volleys it at your aim, members holding formation around you. Possessing a tower is manning the gun: manual aim, manual trigger, doctrine at your thumb. The front keeps fighting under its standing orders while you drive; release (or a bell save) returns you to command view. The enemy needs no mirror rule — their brain already drives their units; possession is you doing for one what it does for all. Heroes, when they arrive, are not a special control scheme — they are simply the best bodies to possess.

## 4. The Units

**Engineers and sappers become two different teams.** Engineers arrive early: tap a line on the ground and they walk it building sandbags; later they repair masonry and build **bridges**. Sappers arrive later with the satchels — and bridges and dams are on their target list. Build it, hold it, blow it.

**Rocket teams are in**: the infantry mirror of the rocket tower — four rockets, a long reload, saturation over precision. They share the artillery rung with mortars so the manifest makes you choose a doctrine.

**Heroes** cap the ladder at the late bells: tanks, an ace sniper, the Bison itself, and finally **the mech**. Heroes are **directly pilotable** — select one and you drive it, while the rest of the front keeps fighting under its standing orders. The enemy fields heroes too, machine-driven: their tank, their ace, their mech. A mech duel over the dam is the poster this game wants on its wall.

Identity: the enemy's marksman pair loses the android silver and dresses like the men they fight beside. Every soldier on both sides now leaves a **red smear** where they die — permanent for the whole match. The snow remembers.

## 5. The Enemy Front

The enemy stops being a wave table and becomes an army run by the same rules: mirrored tiers at the bells, their own manifest picks, and — from the first vision — the **build-brain**: they place towers and walls on ground they hold. Their late-game heroes drive themselves. Their sappers look at your dam the way yours look at theirs.

## 6. The Front (map & terrain)

The field widens from 58m to **about 80m** — room for a third road, real flanking routes, and a proper village on each terrain bench instead of a hamlet.

Buildings get variety, ported from the proving grounds' proven forms: hangars with one-piece slab roofs that *pancake* when dropped, warehouses whose interior columns are the demolition lesson, drive-through barns, and freestanding field walls that give infantry something to fight from between the benches.

Terrain grows teeth:

- **Copses and forests** — cover, concealment, and they burn.
- **Real ice** — the sandbox's cracking, drowning ice comes to every frozen pond. Artillery can deny a crossing.
- **A stream on every map**, seeded anywhere from a creek to a roaring river. Water is modeled as connected basins that *settle* when the ground changes: a crater near the bank floods; blow a channel and a pond drains. No fake fluid physics — just water honestly finding its level when the terrain moves.
- **Bridges** — walkable, engineer-built, sapper-blowable. The chokepoint you can create and the one you can erase.
- **Dams**, on some maps, holding back lakes. Blow one and a flood surge sweeps the valley: troops knocked down and drowned, sandbags and light structures stripped, and then the map is different — the lakebed is crossable, the valley is not. Both sides can do this. Floods drown everyone.

## 7. Presentation & performance

- **30 frames per second by default on phones and tablets** (with a toggle; desktop stays at 60). The physics never changes — this only slows the pictures, and the pixel-art look wears it well. Buys battery and thermal headroom.
- Render streamlining under the hood (details in Part Two) — measured on the Pi before and after, no guessing.
- **Audio** for the new heartbeat: the bell, the convoy's arrival, the flood, the radial's clicks.
- **Weapon-voice identity** *(added 2026-08-12)*: every gun sounds like itself — the bell a single deep BONGGG, the sniper a dry *crack* whose echo grows with distance, the MG a true *ratatata*, rifles pitched above the sniper. The audio engine's distance and echo model already exists; the guns just need their own voices.
- The start screen gains **RESUME FRONT** for saved matches.

## 8. The Order of Work

1. **Cleanup** — ✅ shipped (mk0.30-0.36): test purge, squad-straggler fix, perf baseline (verdict: the game is *physics-bound* — sim-side work must precede or ride the map phase), pace preview.
2. **The Bell** — ✅ shipped (mk0.40-0.43): muster cycle, manifest, bell saves, the bell's sound.
3. *(added 2026-08-12)* **Polish I** — Playtest I's directives: 90s bell, tighter formations, interim cost raise, intel card withdrawn (machinery kept), first-manifest teaching line, off-map order clamp, walls as visible 3-stacks and sandbags as 1 with weld seams shown, weapon-voice identity, minimal engineer v1 + seeded depot sandbags.
4. **Vision** *(moved ahead of Command, 2026-08-12 — the territory targeting gate is the feel problem playtesting surfaced)* — sight-based targeting; one fan.
5. **Command** — the radial and the first order set; tower doctrine.
6. *(added 2026-08-12)* **Possession** — TAKE CONTROL on every friendly radial: squads driven as one (stick = formation anchor, button = volley at your aim), towers as manual fire-control; pilot camera + contextual ability buttons built once, reused by every type including the later heroes. Bison drive/twin-stick precedents in core.js + the sandbox are the reading list's heart.
7. **The Front** — 80m width, building variety, forests, third road — with the sim-side perf work the baseline demands riding alongside.
8. **Engineers & Arms** — the full engineer kit (repair, walls, bridges-ready), sappers as their own unlock, rocket teams.
9. **Water** — real ice, the stream, basins, bridges.
10. **The Dam** — lakes, the surge, the terraform.
11. **The Enemy Front** — build-brain, deeper doctrine, the assault rhythm matured.
12. **Heroes** — tanks/ace/Bison pilotable with enemy mirrors; **the mech last**, once its walk (M3/M4) is done on its parallel track.
13. **Balance** — the held F5 pass runs once the front's shape settles, and builds the **mercenary market** (§2a) as its centerpiece; every cost and income in this document is provisional until then.

Each phase ships playable and bumps the version a tenth. Every phase gets its own approved plan before work starts.

## 9. Documents & process

Rolled into this era: a **systems reference** for implementation agents (what lives where, so every dispatch reads the right code), and a **test manifest** listing every test with a keep/prune verdict for your review — the purge is a decision, not a vibe. Standing process: load-only tests, untouched code stays untouched, Opus 5 agents with reading lists, and no plan ever contains an unanswered question.

---

# PART TWO — Technical Companion (for agents)

*Code anchors verified against the tree at mk0.29 (prod). Read the anchors before touching anything; every phase plan will carry its own reading list drawn from here.*

## T1. The Bell (phase 2)

- **Replaces**: the `WAVES` 50-entry ramp (`src/depot/specs.js:71`) and the `PHASE.BUILD/WAVE/STALL` machine (`src/depot/state.js:112`, consumed in `DepotGame.jsx`'s loop). The bell is a shared clock on `world.t`; continuous play between bells (no build-phase pause).
- **Bell moment ordering**: intel (`src/depot/intel.js` — `composeIntel`/`openingIntel` already exist), income (`src/depot/economy.js` `STIPEND` → becomes cycle income), manifest choice (UI on the brief-card idiom — scrim-backed modal, ACKNOWLEDGE-armed; see the trailing-tap laws below), then assault (spawner draws from the *tier state*, not `WAVES`).
- **Tier state**: one source of truth consumed by the player shop (build menu shows only unlocked), the manifest generator, and the enemy brain (`src/depot/ai.js` `planWave` gains tier caps; enemy manifest picks are seeded rng draws — mind draw-count discipline, `scripts/depot-lint.mjs` forbids unseeded randomness in `src/depot`).
- **Saves**: bell boundary is the quiescent save point. Serialize game-layer state (resources, tier state, squads roster, tower/wall/sandbag placements, depot census, territory field, rng cursor) — **not** raw physics bodies; rebuild the world deterministically from the seed + rebuild log, same philosophy as `scenario.js`. Loss-final: delete save on depot fall. Storage via `window.storage` (`src/platform/storage.js`), key family `coldsnap-wf-*`. START SCREEN: `src/ui/StartScreen.jsx` gains RESUME FRONT when a save exists.
- **Traps**: draw-count stability contracts throughout `src/depot` (e.g. `squads.js` one-draw-per-attack-leg, documented at `squads.js:13`); the resume path must land the rng stream exactly where the save left it — store the mulberry32 state, don't replay.

## T2. Radial & orders (phase 3)

- **Replaces** the chip row (`DepotGame.jsx:2004-2010`) and `orderSquad`/`orderMode` flow (`DepotGame.jsx:1050-1075`, `1168-1180`). Radial opens on placement commit and on squad/tower tap; dismiss = defend-here (already the intrinsic default: `makeSquad` order "defend", `squads.js:50`).
- **Tap discipline**: inherit `PENDING_ARM_S` (0.35s, `state.js:77`) and the mk0.27 visibility rules (`pendingButtonsVisible`, `state.js:95`) — the placement tap's trailing click must not select a radial entry; a radial scrolled off-screen must self-cancel.
- **Order implementations ride existing solvers**: take cover = defend micro-seek (`squads.js:551-593`) with widened radius; patrol = MOVE legs (`squads.js:458-548`) over a waypoint list with threat interrupt; hold fire = squad-level gate in `squadFire` (`state.js:274`); attack structures = invert the unit-first/structure-fallback priority (`state.js:299-324`); fall back = MOVE legs with `squadFire` enabled (drop the `order === "move"` early-return for the retreat order); escort = anchor follows target squad's anchor at offset; suppress/barrage = `shooterFire` at a ground point (needs a point-target variant — `shooterFire` currently takes a body with `.pos/.v/.hy`; a stationary pseudo-target object satisfies the interface); directed demolition = sapper target override in `stepSapperCharges` (`squads.js:414`).
- **Tower doctrine**: per-tower fields read by the acquisition scan in `DepotGame.jsx`'s `stepTowers`; `friendlyFouls` (`state.js:493`) already implements careful. Sector = bearing filter on acquisition; focus = pinned target id with revalidation.
- **Cohesion time-cap** (cleanup phase, Jeff-ratified): bound the `trail > COHESION_M` anchor hold (`squads.js:529-537`) by a per-leg timer (a few seconds), then advance. Straggler root cause documented in the 2026-08-11 flake autopsy (deterministic detour-fan oscillation in concave pockets; `slotBlocked` ignores `invM > 0` rubble — `squads.js:144`).

## T3. Units & arms

- **Engineer/sapper split**: `SQUAD_SPECS` (`squads.js:17`) gains `engineers`; sandbag-line order = waypoint walk + `spawnSandbag` (`state.js:385`) per step with a build-time delay and scrap cost; field-built bags should be *slower + cheaper* than the instant build-menu bag (design intent). Repair/bridge-building are later rungs of the same unit.
- **Rocket teams**: `INFANTRY_ARMS.rockets` derived from `TOWER_SPECS.rocket` (`specs.js:16`) at the halved cadence; `burst: 4` rides `squadFire`'s existing volley path (`state.js:287`); blastR/kv real (unlike the mg-kind fallback); troopkit launcher prop (`src/render/troopkit.js` — follow the MORTAR_TUBE pattern, no preRot shear on scaled props); enemy mirror in `ENEMY_SPECS`/`ai.js`. No friendly-fire check exists in `squadFire` — flagged for Jeff's playtest, not silently added.
- **Artillery halving**: `TOWER_SPECS.mortar.fireRate 2.3→4.6`, `rocket 4.4→8.8`, `INFANTRY_ARMS.mortars.fireRate 3.0→6.0`, `ENEMY_FIRE.lob.cd 3.0→6.0` (symmetry). Re-measure DPS parity baselines in `scripts/depot-test.mjs` rather than tuning around them.
- **Smears**: set `smearStyle = "human"` at every infantry spawn site (`spawnSquadMembers` `state.js:349`, `spawnUnit` `units.js:23` — including the marksman pair). **Permanence requires work**: the DEPOT fade pass (`renderer.js` `splat.fade`, armed via `opts.fadeDecals`) re-blends the clean base over the *whole* canvas, which would grey smears too. Keep a smear ledger (positions/styles) and repaint after each fade tick, or composite smears on their own canvas — either way the fade must not touch them (Jeff: permanent).
- **Marksman re-dress**: drop `dress: "android"` from `ENEMY_SPECS.sniper` (`specs.js:35`) — troopkit then palettes them slate by team automatically (`troopkit.js:127`). Check nothing pins the android dress in tests.
- **Heroes**: Bison drive/fire = `driveHull`/`bisonFire`/`bisonMg`/`recoverBison` (`core.js:877-2361`) — DEPOT never fields it; pilot mode = `world.control` + camera follow (sandbox twin-stick precedent, `ContractSandbox.jsx:507-529`). Player tank = team-1 `spawnTank` variant with `aiDrive` goal-seek when not piloted. Mech hero gated on M3/M4 (`src/engine/mech.js`, parallel track; enemy mech needs an AI driver over `mechCommand`/`mechFire`/`mechMissiles`). Enemy hero tank driver exists (`stepTank`, `units.js:110`). Piloting shifts input mode while squads/towers continue under standing orders — the depot loop already separates command state from sim state.

## T4. Map & terrain

- **Width**: canonical half-width 29 → ~40. The literals to thread through one constant: terrain falloff box (`DepotGame.jsx:194` — `|u|-29`, `|v|-57`), renderer rim opts (halfU 29/halfV 57), `genMap` placement ranges (`rocks x ±25` :49, buildings `±21` :119, ponds `±18` :76, spawns), flow-grid extents (`GRID_W/GRID_H/GRID_OZ`), territory grid, `world.inRim`. Spawn count/spread widens with the field.
- **Buildings**: port PG forms into the `TPL` list (`DepotGame.jsx:83`) — slab-roof hangar (rigid slab welded to top two courses, slab sized *inside* the wall-cap ring with the ~2cm joint: `core.js:2210-2236`), warehouse columns (`core.js:2239-2257`), drive-throughs, field walls (`buildCoverWalls` pattern, `core.js:2135`). `buildTown` (`DepotGame.jsx:413`) needs per-template shape hooks beyond the uniform lattice. **Chunk budget**: CHUNK_CAP 2000 (`renderer.js:715`) — HUD stones counter is the alarm; measure worst-case collapse on the Pi before raising density.
- **Trees**: copse/forest placement in `genMap`; raise the 144-instance tree pools (`renderer.js:1063-1075`).
- **Ice**: generalize `freezePool` (`core.js:1186` — hardcoded to the demo `POOL` rect) into a depot-side per-pond plate builder (lattice + rim anchors pattern is portable). Drowning needs water regions: guarded core divergence replacing the single `world.water` rect with a list (every consumer: `stepStatus` drown/swim `core.js:1775-1793`, buoyancy `:1760`, `stepUnits` wet checks — the POOL-hardcoded panic paths are demo-AI only; depot units are `brave` and skip them, verify per site). Lock with a righting-test extension, golden untouched (additive, guarded).
- **Basins**: streams/rivers as chained basin segments over the heightfield; deterministic settle pass on `field.carve` events (territory-tick cadence, not per frame); flow-grid and territory must re-read water cells after a settle. Renderer: per-basin water planes (stepped for gradient); the single `water` mesh (`renderer.js:522`) generalizes.
- **Bridges**: chunk-lattice decks on piers; units cross via pair-contact grounding (divergence #6a, `core.js:1377-1384` — built for exactly this). Engineer-built = staged construction (like sandbag lines); sapper target via `hostileStructure` extension. Flow-field: bridge cells walkable while deck stands, blocked when dropped — census pattern like `stepTown` (`DepotGame.jsx:486`).
- **Dams**: masonry lattice holding a high basin; breach detection via the standing-census pattern (`censusDepotChunks`/`standingStructure`, `state.js:499+`); on breach → basin connect + settle + **surge event**: a deterministic front sweeping the downstream basin path over seconds, applying impulses + `hitT` knockdowns (drown clock kills), weld-shock along the path (the `explode` weld-`acc` pattern without the blast), then persistent water-region and flow-grid updates. No rng anywhere in the surge.

## T5. Presentation & perf

- **30fps**: rAF-skip in the DEPOT loop only (sim accumulator unchanged at `dt = 1/120`; step cap already exists). Touch default on (`detectTouch`), menu toggle, desktop 60.
- **Perf order (measure on Pi first, then)**: (1) skip the normals render pass entirely when `gfx.outline === 0`, else render it at half RT res (`renderer.js:1874-1880`); (2) cache instance writes for sleeping chunks/dead units (compose only on wake/move; `renderer.js:1513-1521` chunk loop, infantry loop `:1394`); (3) typed body sub-lists maintained by the depot layer (live units, standing structures) to shorten the full-`world.bodies` scans in `squadFire`/`stepRifleman`/`exposureAt`/`slotBlocked` hot paths. Game-layer only; core.js untouched.
- **Audio**: new events through `makeGameAudio.consume` (`src/platform/audio.js`) — bell, manifest arrival, flood surge, radial ticks; events are not hashed (safe).

## T6. Enemy front

- **Tiering**: `ai.js` `planWave` capped by tier state (T1); enemy manifest picks per bell (seeded draws, count-stable).
- **Build-brain (F3)**: enemy tower/wall placement on their held ground — `hostileStructure` team-1 branch is already F3-ready (`state.js:263-271`); placement legality mirrors `validatePlacement` on the flipped territory read.
- **AI heroes**: tank driver exists; ace sniper = pair machinery both signs already (`units.js`/`squads.js` shared solvers); mech driver = new policy over the mech command API.

## T7. Cleanup phase specifics

- **Test manifest** (Jeff-reviewed doc): inventory `scripts/*.mjs` (12,340 lines) with keep/prune verdicts. Prune scripted-gameplay bots (incl. the flaky rotated-advance and fire-discipline-reload smoke sections — autopsy closed, artifacts in `/tmp/flake/`). Keep: `golden.mjs` (frozen-demo law), headless logic gates that pin engine/game *invariants* cheaply, and boot/load smoke (each surface mounts, no console errors). New-work verification = load checks + Jeff's playtest, per the standing rule.
- **Also in cleanup**: cohesion time-cap (T2), artillery halving, smears + marksman re-dress, 30fps toggle — the pace-preview wins; Pi perf baseline (frame-time split sim/render, worst-case collapse, chunk counts); Jeff's outstanding playtest confirmations recorded.
- **Traps standing for every phase**: `depot-lint` seeded-rng law; draw-count stability contracts; `PENDING_ARM_S`-family tap arming on any new in-world UI; version bump every deploy (`src/version.js`, +0.01/task, +0.1/phase); provisional costs stay marked `// provisional (F5)`.

## T7a. The mercenary market *(added 2026-08-12 — built in the Balance phase)*

- Per-item reserve pools + bonding-curve price (constant-product shape), shared by BOTH armies (one market, symmetry at its purest); reserves replenish per bell. Deterministic — the curve is pure math on the reserve, no draws.
- The enemy pays market prices too: `planWave`'s budget spends against the same curves — its buy choices shift with prices exactly like the player's should. Order of resolution is real-time FCFS (sim-time ordered, deterministic); the enemy brain staggers its purchases across the cycle (a single bell-batch would be predictably front-runnable every cycle).
- Territory → replenishment: per-side refill rate scales with held-ground fraction (territory.js's field is the input); symmetric rule, no price asymmetry.
- Global reserve totals are sized against the physics budget (the C0 baseline's sim ceilings) — the market is the perf governor; no visible caps.
- Interim until built: flat costs raised ~50% (Polish I) hold the line.

## T8. Documents

- **Systems reference** (`docs/superpowers/systems.md`): one page per system with file anchors — the standing source for per-phase reading lists.
- **Test manifest** (`docs/superpowers/test-manifest.md`): the keep/prune table.
- Per-phase plans continue in `docs/superpowers/plans/`, two-part, each carrying its reading list and trap notes per the dispatch rules.

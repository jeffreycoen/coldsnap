# The Trimmed Range — T1, Amendment 1: the seven dead pointers

The step-3 grep found seven comment-only references to the deleted
files in kept code. They are scrubbed: each comment loses its dead
file reference; any constraint text around it stays.

| file : line | disposition |
|---|---|
| `src/game/runner/trials.js:2` | the "Moved verbatim from CampaignRunner.jsx…" sentence deleted (provenance only) |
| `src/game/runner/Typed.jsx:3` | same |
| `src/depot/DepotGame.jsx:74` | the ContractSandbox.jsx naming goes; the discipline statement ("pushed straight to the DOM… so a drag never queues a re-render") stays |
| `src/depot/DepotGame.jsx:262` | the CampaignRunner naming goes; the persistence-pattern statement stays |
| `src/depot/DepotGame.jsx:914, 979, 1755` | the ContractSandbox.jsx citations (file and line numbers) go; each comment's own behavioral statement stays |

Comments only — a changed code line stops the task. After the scrub,
the step-3 grep is re-run and must be clean as originally written.
Everything else in the approved plan stands.

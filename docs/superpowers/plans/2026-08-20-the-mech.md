# THE MECH — task plan (proposed mark mk1.92)

*Written 2026-08-20 on the owner's word. One task, the next open mark (mk1.91 shipped → mk1.92); if the owner rules a split at review, the phases below cut cleanly at Phase F (machine in the war) / Phase G (interface and cards). Suggested model: Sonnet — every step is specced to the code; the agent executes, never designs. The probe is the first gate: its numbers open the report, and a fail stops the task before any fielding code runs.*

## The twelve standing rulings (owner, 2026-08-20 — this plan's design truth)

1. **BOTH SIDES NOW** — the enemy fields and drives the mech in this same task; its driver is built from the tower-defense boss precedent through the motor pool.
2. **HERO CARD AT PLAIN ODDS** — it arrives through the opening draft and the convoy hand like the Bison and the APC, one table both sides.
3. **KNOCKDOWN** — helpless a few seconds, then stands where it fell; damage taken while down is real; the fall itself never wounds it.
4. **BOXES FIRST** — the plain steel look ships; the art is its own later work.
5. **BIG AND PHYSICAL** — real contacts with objects and units, like everything else.
6. **ONE ADDED WEAPON: THE HEAVY SALVO** — a heavier missile mode on a long cooldown; no machine gun, no frost bolt, no stomp.
7. **THE FEET OBEY THE TRACKS LAW** — enemy men die underfoot; own men are braked for; careful/free, both sides.
8. **SIZE STAYS** — ~7 m, ~19 t, exactly as the walk was certified.
9. **PRICE ~400 SCRAP** — hero market family, the wall on top; a drafted mech is a lucky war, accepted knowingly.
10. **ROCKETS ON** — stabilization rockets and speed assist ship enabled (~0.7 m/s cruise).
11. **FULL CITIZEN UNPOSSESSED** — radial orders (defend, move, patrol, escort) through its own driver row; possession optional.
12. **THE PROBE IS THE FIRST GATE** — the Pi measurement, worst case two mechs fighting amid the full standing field, before any fielding code.

## Required reading (agent, before any code; anchors re-verified at dispatch)

This plan whole; `CLAUDE.md`; `src/engine/mech.js` whole; `src/engine/core.js` — `applyDamage`/`explode`/projectile-hit region (~:560-830), the crush/slam hooks (~:1740-1780), the mech island hooks (:1476, :1523, :1598, :2004-2007), `__mech__` (:2488); `src/game/ColdsnapTD.jsx` :860-950 (the boss driver — the template); `src/game/MechRange.jsx` whole (the control scheme being ported); `src/depot/drivers.js` whole; `src/depot/muster.js` — `parkArmor`/`PICK_POOL`/`draftDeal`/`mirrorFieldKey`; `src/depot/bell.js` — the hero-replacement block (~:131-141); `src/depot/specs.js` — `BISON`/`APC`/`HAND_KEYS`/`HAND_TAGS`; `src/depot/market.js` whole; `src/depot/lists.js` whole; `src/depot/save.js` whole; `src/depot/sight.js` :19-35; `src/depot/state.js` — `squadFire`'s foe scan (:559), `snapTargetNear` (:628-640), `executeWithdrawal`'s sweep (~:1599); `src/depot/units.js` — `payBounties` (:85-95); `src/depot/squads.js` — the hull-avoid steer (:208), `slotBlocked`'s hull test (:505), the mechanic target list (:894); `src/depot/DepotGame.jsx` — `tapAt`'s candidate scan (~:2275), the vehicle radial + `takeControlVehicle`/`orderVehicle` (~:1700-1745), the frame loop's possessed blocks (~:3084-3175, ~:3339-3365), `buildEmitters` (~:1095), `placePick`/`placeHire` hull branches (~:1631, ~:2587), the resume branch (~:990-1067, ~:1358-1387), the debug hooks (~:2704+); `src/render/renderer.js` — the mech block (~:897-899, ~:1899-1960), the health-bar loop (grep `barFillMesh`), `setTraj`; `src/render/portrait.js` whole; `src/ui/FieldManual.jsx` — card list + `MANUAL_REV`; `scripts/depot-test.mjs`; `scripts/tests/harness.mjs` + `shared.mjs`; the six count-pin sites in the ledger below; `.superpowers/p7-cap-fine.mjs` (the probe idiom).

## Trap notes

- **The save's generic sweep would serialize mech pieces as loose boxes.** Phase E filters them out and writes one mech row. The weld writer already skips welds whose bodies are missing, so the head weld drops cleanly.
- **Every targeting scan filters by body kind.** The mech is invisible to all of them until each Phase D site is taught. Do not "fix" scans the plan does not name — enemy riflemen do not target hulls today and must not target mechs either (consistency, not an oversight).
- **Crush and ram attribution in core.js is hardcoded to the player** (:1747, :1770-1771). Phase B makes it team-aware. These are guarded divergences — the frozen demo has no mech bodies, so `golden.mjs` cannot move.
- **The walker's own gates are pinned thruster-free.** The war enables thrusters per ruling 10; nothing under `scripts/` that certifies the gait may be touched.
- **`executeWithdrawal`'s sweep filters on kind unit/vehicle** — the mech is excluded by construction and must never be withdrawn. Verify, don't change.
- **`placeMech`/`respawnMech` do not touch hull hp** — that is what makes stand-back-up preserve wounds. Verify at Phase F, don't re-zero.
- **A fall must not wound** (ruling 3). After Phase B lands, verify with the knockdown fixture that no contact-damage path drains hull hp during a faceplant; if one does, gate that path off for `b.mechRef` bodies and report it as a named deviation.
- **Adding the eighteenth key shifts fixed-seed draft VALUES, never draw counts.** `draftDeal` draws exactly 7 whatever the pool size. The value-shift re-base license below covers boot-deal fixtures; every draw-count pin must hold unmoved.

## Dials (all provisional, F5, tuned by play)

| Dial | Value |
|---|---|
| `MECH.hp` | 900 |
| `MECH.cost` | 400 |
| `MECH.bounty` | 120 |
| Sight row `SIGHT.mech` | 40 (eye at hull + 2.6 m) |
| Heavy salvo | 9 rockets, 30 s cooldown, 7 m spread, rocket dmg/blast as the existing salvo |
| Driver gun scan reach | 40 m; fire when torso within 0.12 rad of the aim |
| Driver barrage trigger | target is a structure, or ≥4 enemies within 8 m of the aim point |
| Knockdown helpless window | 6 s + 5 s stand grace |
| Walk commands | travel 0.9 straight / 0.35 turning (the boss numbers) |
| Park vet footprint | half-extents 2.2 × 2.2, spread < 0.28, clearance ring 4.5 m |

---

## PHASE A — the probe (the first gate)

**Step A1 — the staging hooks.** `src/depot/DepotGame.jsx`, beside `window.__DEPOTSQUAD__` (~:2822): two debug hooks — field a mech for either team at a point, and read every mech's state. (These land before the probe because the probe drives them; they are harness surface only, cost-free, and the pattern of every `__DEPOT*__` hook.)

```js
window.__DEPOTMECH__ = (team, x, z, yaw) => {
  const m = buildMech(world, { x, z, yaw: yaw || 0, team: team || 1, hp: MECH.hp });
  m.thrustersOn = true; m.thrustAssist = true;
  m.hull.drv = "mech"; m.hull.order = "defend"; m.hull.tracks = "careful";
  m.hull.maxHp = m.hull.hp; m.hull.homeX = x; m.hull.homeZ = z;
  if (team === 2) m.hull.bounty = MECH.bounty;
  return m.hull.id;
};
window.__DEPOTMECHS__ = () => (world.mechs || []).map((m) => ({
  id: m.hull.id, team: m.team, hp: +m.hull.hp.toFixed(1), mode: m.state.mode,
  x: +m.hull.pos.x.toFixed(2), z: +m.hull.pos.z.toFixed(2),
  falls: m.telem.falls, steps: m.telem.steps, order: m.hull.order || null,
}));
window.__DEPOTMECHORDER__ = (id, kind, x, z) => {
  const b = world.byId.get(id); if (!b || !b.mechRef) return null;
  b.order = kind; b.dest = x != null ? { x, z } : null; b._route = null; b._routeDest = null;
  return { order: b.order, dest: b.dest };
};
```

(`buildMech` and `MECH` join this file's imports in Phases B/D; until then these hooks are added but the file will not compile — Step A1 is WRITTEN as part of Phase D's landing and only DESIGNED here. The probe run happens after Phase F, before Phase G: the gate is "no interface work until the machine passes." This ordering is deliberate and reported as such: the probe needs the driver to make the mechs fight.)

**Step A2 — the instrument.** New file `.superpowers/mech-probe.mjs`, the `p7-cap-fine.mjs` idiom verbatim (same launch, same seed 2307, same `?perf=1` + normalized arithmetic): boot, `__DEPOTSTART__`, stage the standing war (8 rifle squads at the depot front + `__DEPOTSPAWN__(6)` × 8 waves ≈ 60–70 men), then `__DEPOTMECH__(1, …)` and `__DEPOTMECH__(2, …)` 60 m apart, ordered onto each other with `__DEPOTMECHORDER__(id, "move", …)`. Measure a 12 s window (settle 8 s first); then fire six `__DEPOTSHELL__` at one mech to force a knockdown and measure a second 12 s window covering the down-and-stand. Report both windows' normalized cost. Two repeats within 3%.

**Step A3 — the gate.** Both windows ≤ 11.0 ms normalized on the Pi, two repeats. The numbers open the task report. Over the line = STOP: report the numbers, no fielding code ships, the owner rules.

## PHASE B — the engine learns two sides (guarded divergences; golden stays green)

**Step B1 — the team rides the machine.** `src/engine/mech.js` `buildMech`: the hull takes the caller's team and the mech object remembers it; every link carries a routing hp.

- In `buildMech`, hull body creation (~:497): `team: 1` → `team: opts.team || 1`; and after `mech.mass` is set, add `mech.team = opts.team || 1;`
- In the `B()` helper's link bodies (yaw/hip/thigh/shin/ankle/foot/torso/head/arms): add `hp: 1e9` to each `addBody` call (finite, so every damage path reaches the routing clause below).

**Step B2 — one wound ledger.** `src/engine/core.js`, first line of `applyDamage`'s body:

```js
// DIVERGENCE (guarded: no mechRef in the frozen demo): a mech takes every
// wound on ONE ledger — any hit on any limb is a hit on the hull.
if (b.mechRef && b.mechRef.hull && b !== b.mechRef.hull) b = b.mechRef.hull;
```

The existing projectile-hit rerouting at ~:808-811 becomes redundant with this and is LEFT IN PLACE untouched (it also carries impact-impulse semantics; verify, don't consolidate).

**Step B3 — team-aware attribution.** `src/engine/core.js`:

- Crush (~:1747): `attacker: other.id === world.bisonId || other.kind === "mechfoot" ? "player" : "world"` → `attacker: other.kind === "mechfoot" ? (other.mechRef && other.mechRef.team === 2 ? "enemy" : "player") : other.id === world.bisonId ? "player" : "world"`
- Ram (~:1771): the `other.mechRef ? "player"` clause → `other.mechRef ? (other.mechRef.team === 2 ? "enemy" : "player")`

**Step B4 — the guns learn their side.** `src/engine/mech.js`:

- `mslAimPoint` (~:2843): `if (!b.alive || b.team !== 2) continue;` → `const foe = (mech.team || 1) === 1 ? 2 : 1;` above the loop, filter `b.team !== foe`.
- `mechFire` (~:2768) and `mechMissiles` (~:2881): `attacker: "player"` → `attacker: (mech.team || 1) === 2 ? "enemy" : "player"` (both `fireProjectile` calls).

**Step B4b — AMENDMENT 2 (2026-08-20, after the agent's second honest stop — the gap was the plan-writer's): the wounds actually arrive.** The plan taught `applyDamage` to route mech wounds to the hull but never taught the two damage GATES that a mech exists — both filter on kind unit/vehicle/truck, so no weapon in the game could wound a machine. Two guarded additive edits in `src/engine/core.js`:

- **Direct hits** — `stepProjectiles`' direct-hit clause (~:765): the kind filter gains the three mech kinds: `... || hitBody.kind === "mech" || hitBody.kind === "mechlink" || hitBody.kind === "mechfoot"`. The Phase B ledger reroute delivers any struck piece's damage to the hull, armor-consulted once, attribution intact.
- **Blasts** — one blast, one wound, by hull proximity (the boss pool's own proven shape, paid through `applyDamage` so attribution and the kill event ride the normal path). Immediately after the existing bossHp block (~:665-670):

```js
// DIVERGENCE (guarded, A2): a WAR mech's hull carries real hp — each blast
// wounds the machine ONCE, by hull proximity (the boss pool's shape), through
// applyDamage so attribution, lastHit, and the kill all ride the normal path.
// A boss (bossHp) keeps its own pool untouched; a direct-hit round's blast
// component never double-pays the body it already struck.
if (world.mechs) for (const mm of world.mechs) {
  if (!mm.hull || !mm.hull.alive || mm.bossHp != null) continue;
  if (!spec.dmg || mm.hull.id === spec._directHitId) continue;
  const dd = Math.hypot(mm.hull.pos.x - x, mm.hull.pos.y - y, mm.hull.pos.z - z);
  const reach = spec.r + 3.5;
  if (dd < reach) applyDamage(world, mm.hull, spec.dmg * Math.max(0, 1 - dd / reach), { cause: CAUSE.BLAST, attacker: spec.attacker || "world", volley: spec.volley || 0, srcId: world.depotCombat ? spec.owner : undefined, srcX: world.depotCombat ? x : undefined, srcZ: world.depotCombat ? z : undefined });
}
```

Range mechs (hp 1e9) lose nothing perceptible; the frozen demo has no mechs, so golden cannot move. M19 becomes literally true: shells landing on a downed machine drain its ledger. The agent's Phase F–H interpretive completions reported at this stop (desktop mouse aim via `groundPoint`, W/S+A/D keys without a strafe binding, the exported `mechSighted` gate, the ~8Hz range-slider state, the unlabeled right stick) are RATIFIED as plan text.

**Step B5 — asserts (era file, reds first — see Phase J for the file).** Checks M1–M4: a team-2 build carries team 2 on hull and mech; a shell into a thigh drains the hull's ledger and never the link's; `mslAimPoint` on a team-2 mech locks a team-1 body; a mechfoot crush by a team-2 mech attributes "enemy".

## PHASE C — the heavy salvo

**Step C1 — `mechBarrage`.** `src/engine/mech.js`, after `mechMissiles`: nine rockets, three ripples of three, deterministic ring spread around the aim point (no rng — the engine's law), per-rocket solve and recoil copied from `mechMissiles`' own math, 30 s cooldown, 10 m danger-close refusal, pod-tracking stamp shared with the salvo.

```js
// THE HEAVY SALVO (owner, 2026-08-20): the one added weapon — a saturation
// barrage on a long cooldown. Nine rockets walk a deterministic ring around
// the aim point; each flies the salvo's own fixed-loft solve. No rng.
export function mechBarrage(world, mech) {
  if (mech.state.mode === "FALLEN") return false;
  if (world.t - (mech._lastBar || -99) < 30) return false; // provisional (F5)
  const torso = mech.waist ? mech.waist.b : mech.hull;
  const tgt = mslAimPoint(world, mech);
  if (Math.hypot(tgt.x - torso.pos.x, tgt.z - torso.pos.z) < 10) return false; // danger-close
  mech._lastBar = world.t;
  mech.mslTarget = tgt; mech._lastMsl = world.t; // the pod tracks the strike
  const Rm = torso.R;
  const m0 = { x: torso.pos.x + Rm[0] * -1.35 + Rm[3] * 1.05, y: torso.pos.y + Rm[1] * -1.35 + Rm[4] * 1.05, z: torso.pos.z + Rm[2] * -1.35 + Rm[5] * 1.05 };
  const SPREAD = 7, N = 9; // provisional (F5)
  for (let i = 0; i < N; i++) {
    const ring = i === 0 ? 0 : i < 4 ? 0.5 : 1.0;
    const a = (i * 2.399963) % (Math.PI * 2); // golden-angle walk — even, deterministic
    const ax = tgt.x + Math.sin(a) * ring * SPREAD, az = tgt.z + Math.cos(a) * ring * SPREAD;
    const dx = ax - m0.x, dz = az - m0.z, d = Math.max(6, Math.hypot(dx, dz));
    const h = m0.y - world.field.heightAt(ax, az);
    const g = world.gravity, tanTh = 0.62;
    const den = 2 * (d * tanTh + Math.max(0.5, h));
    const spd = Math.min(90, Math.max(10, Math.sqrt(Math.max(100, g * d * d * (1 + tanTh * tanTh) / den))));
    const th = Math.atan(tanTh), cp = Math.cos(th), sp2 = Math.sin(th);
    const ux = dx / d, uz = dz / d;
    const dir = v3(ux * cp, sp2, uz * cp);
    V.norm(dir, dir);
    fireProjectile(world, v3(m0.x + ux * 0.6, m0.y, m0.z + uz * 0.6), dir, spd,
      { kind: "rocket", pmass: 15, r: 3.2, kv: 13, dmg: 62, crater: 0.8, attacker: (mech.team || 1) === 2 ? "enemy" : "player", ownerMech: mech, delay: i * 0.22, vroll: true });
    torso.v.x -= dir.x * (15 * spd) * torso.invM;
    torso.v.z -= dir.z * (15 * spd) * torso.invM;
  }
  wake(torso);
  mech.telem.barrages = (mech.telem.barrages || 0) + 1;
  return true;
}
```

**Step C2 — asserts.** Checks M5–M6: nine rockets fired, cooldown refuses a second call inside 30 s; a call inside 10 m refuses (danger-close).

## PHASE D — a citizen of the war

**Step D1 — the war spec.** `src/depot/specs.js`, after `APC`:

```js
// THE MECH (owner, 2026-08-20): the crown machine, both sides — the engine
// walker fielded as a hero. Dear, slow, unanswerable except by another.
export const MECH = { hp: 900, cost: 400, bounty: 120 }; // provisional (F5)
```

`HAND_KEYS` gains `"hero_mech"` (last); `HAND_TAGS` gains `hero_mech: "hero_mech"`. The comment "The mech joins this list in its own task, not before." is DELETED (its task is now).

**Step D2 — the sight row.** `src/depot/sight.js`: `SIGHT` gains `mech: 40, // the crown's tall eye // provisional (F5)`; `eyeOf` gains, before the vehicle line: `if (b.kind === "mech") return { x: b.pos.x, y: b.pos.y + 2.6, z: b.pos.z, r: SIGHT.mech };`

**Step D3 — the pools.** `src/depot/lists.js` `rebuildBodyLists`: the unit/vehicle branch condition becomes `if (k === "unit" || k === "vehicle" || k === "mech")`, and the vehicles push becomes `if (k === "vehicle" || k === "mech") L.vehicles.push(b);` (the hull joins the spawn-standoff pool; links are kind `mechlink`/`mechfoot` and stay out of every pool by construction).

**Step D4 — the target scans, site by site.** Each edit is the kind filter only; every other predicate stays.

- `src/depot/DepotGame.jsx` `stepTowers`, both sites (:140 revalidation, :162 scan): `(best.kind !== "unit" && best.kind !== "vehicle")` → add `&& best.kind !== "mech"` (same shape at both).
- `src/depot/state.js` `squadFire` foe scan (:559): add `&& e.kind !== "mech"` to the exclusion.
- `src/depot/state.js` `snapTargetNear` (:632): add `&& b.kind !== "mech"`.
- `src/depot/drivers.js` `armorScanFoes` (:321): `(e.kind !== "unit" && (unitsOnly || e.kind !== "vehicle"))` → `(e.kind !== "unit" && (unitsOnly || (e.kind !== "vehicle" && e.kind !== "mech")))` (the coax stays men-only; the main gun engages mechs).
- `src/depot/units.js` `payBounties` (:92): `(b.kind === "unit" || b.kind === "vehicle")` → add `|| b.kind === "mech"` (a killed enemy mech pays its bounty).
- `src/depot/squads.js` :208 (inside `slotBlocked` — A1: label corrected; line and literal text were right): `b.kind !== "vehicle"` → `b.kind !== "vehicle" && b.kind !== "mech"` (men keep clear of the standing machine).
- `src/depot/squads.js` :505 (inside `squadThreatened` — A1: label corrected): same addition.
- `src/depot/squads.js` mechanic target list (:894): `b.kind === "vehicle" || ...` → add `b.kind === "mech" ||` (the paid wrench mends the mech — a machine like any other).

**Step D5 — territory and the hud.** `src/depot/DepotGame.jsx` `buildEmitters`: after the two vehicle clauses, `else if (b.kind === "mech" && b.alive) { const c = invW(b.pos.x, b.pos.z); out.push({ x: c.u, z: c.v, w: EMIT.vehicle.w, r: EMIT.vehicle.r, sign: b.team === 2 ? -1 : 1 }); }`

**Step D6 — the health bar.** `src/render/renderer.js`, the health-bar collection loop (grep `barFillMesh` / the hurt-only sweep): the kind set gains `"mech"` (hull only — it is the one mech kind carrying `maxHp`).

**Step D7 — asserts.** Checks M7–M12: `eyeOf` on a mech hull returns r 40 at +2.6; the pools carry a mech hull in `foes` and `vehicles`; a tower acquires and fires on a seen enemy mech hull; `squadFire` engages one; `armorScanFoes` returns one for the Bison's gun; `payBounties` pays 120 on a dead team-2 hull.

## PHASE E — the save

*AMENDMENT 1 (2026-08-20, after the agent's honest stop — the defect was the plan-writer's): Step E1's row used the key `x` for BOTH the world position and the orders bag, so the bag overwrote the position on every save, and Step E2 read the same corrupted key both ways. The orders bag now lives under its own key `ex`, both sides. Also ratified from the agent's stop report: the ram-attribution rewrite as an equivalent expression; the two swapped site labels in Phase D4 (corrected below in place); the two literal-but-redundant scan exclusions stand as applied; Phase D6's health-bar call is `pushBar(b, 3.2, 7.5)` inside the mech draw loop (values now plan text, provisional F5); the ~20-line portrait builder stands as applied.*

**Step E1 — the writer skips the machine and writes its row.** `src/depot/save.js` `serializeFront`: build the body list as `const list = world.bodies.filter((b) => !b.mechRef);` and index/write over `list` everywhere the function used `world.bodies` (idx map, bodies array, weld rows — welds referencing skipped bodies drop via the existing `a == null` guard). Add to `data`:

```js
mechs: (world.mechs || []).map((m) => {
  const o = { x: r3(m.hull.pos.x), z: r3(m.hull.pos.z),
    yaw: r4(Math.atan2(m.hull.R[6], m.hull.R[8])), hp: r3(m.hull.hp), tm: m.team || 1 };
  const ex = {}; // A1: the orders bag rides its OWN key — never the position's `x`
  let any = false;
  for (const key of ["drv", "order", "tracks", "homeX", "homeZ", "bounty", "maxHp", "escortId"]) {
    const val = plainValue(m.hull[key]); if (val === undefined) continue; ex[key] = val; any = true;
  }
  if (m.hull.dest) { ex.dest = { x: r3(m.hull.dest.x), z: r3(m.hull.dest.z) }; any = true; }
  if (m.hull._patA) { ex._patA = { x: r3(m.hull._patA.x), z: r3(m.hull._patA.z) }; any = true; }
  if (m.hull._patB) { ex._patB = { x: r3(m.hull._patB.x), z: r3(m.hull._patB.z) }; any = true; }
  if (any) o.ex = ex;
  return o;
}),
```

A DEAD mech's loose pieces have had `mechRef` stripped (Phase F death) and ride the generic sweep as plain boxes — a wreck is a wreck, stated and accepted.

**Step E2 — the reader stands it back up.** `src/depot/DepotGame.jsx` resume branch, after `restoreWelds` and the bag re-stamp:

```js
// THE MECH RESUMES STANDING (owner's save law: never raw physics) — rebuilt
// at its spot and heading with its wounds, orders back on the hull.
for (const ms of RES.mechs || []) {
  const m = buildMech(world, { x: ms.x, z: ms.z, yaw: ms.yaw, team: ms.tm, hp: ms.hp });
  m.thrustersOn = true; m.thrustAssist = true;
  m.hull.maxHp = MECH.hp;
  if (ms.ex) for (const k in ms.ex) m.hull[k] = ms.ex[k]; // A1: the orders bag, own key
}
```

**Step E3 — asserts.** Checks M13–M15: a serialized world with one wounded, ordered mech carries one `mechs` row and ZERO mech-piece bodies; the restore rebuilds a standing mech with the same hp, team, order, and destination; a dead mech's wreck pieces save and restore as plain bodies with no mech row.

## PHASE F — the driver row and the machine's death

**Step F1 — the motor pool takes mechs.** `src/depot/drivers.js` `stepDrivers` (:401): `if (b.kind !== "vehicle" || !b.alive) continue;` → `if ((b.kind !== "vehicle" && b.kind !== "mech") || !b.alive) continue;` (the possessed skip and cooldown decay lines hold as written — mech cooldowns are timestamps and need no decay).

**Step F2 — `DRIVERS.mech`.** `src/depot/drivers.js`, after the APC row. Goal policy: the armor's route-leg bookkeeping actuated through walker commands and the boss's heading slew; the tracks-law brake reusing `armorBlockers` and the yield; knockdown tending per ruling 3. Guns policy: one sight-gated scan; cannon when the torso bears; salvos by the dials.

```js
// ---- THE MECH (owner, 2026-08-20): the crown's seat. Goal = the armor's
// route legs actuated as walker commands (the tower-defense boss precedent:
// heading slewed, travel cut through turns). Fall tending per the ruling:
// helpless, then stands where it fell — the fall itself never wounds.
import { buildMech, mechCommand, respawnMech, mechFallen, mechFire, mechMissiles, mechBarrage, mechAimDir } from "../engine/mech.js";
const MECH_ARRIVE = 3.5, MECH_DOWN_S = 6, MECH_GRACE_S = 5; // provisional (F5)
const MECH_GUN = { range: 40, occl: "arc", projSpeed: 120 }; // scan pseudo-spec // provisional (F5)
function mechFallenTend(world, b) {
  const m = b.mechRef;
  if (!m._downT) m._downT = world.t;
  if (world.t - m._downT < MECH_DOWN_S) return;       // helpless — shells land, nothing answers
  let rx = b.pos.x, rz = b.pos.z;
  const wp = b._route && b._route.length ? b._route[0] : b.dest;
  if (wp) { // repeated falls in one patch stand up FURTHER along the route (the boss lesson)
    const dd = Math.hypot(wp.x - rx, wp.z - rz) || 1;
    const adv = Math.min(2.5 + (m._fallN || 0) * 3, dd);
    rx += (wp.x - rx) / dd * adv; rz += (wp.z - rz) / dd * adv;
  }
  m._fallN = (m._fallN || 0) + 1; m._downT = 0;
  respawnMech(world, m, rx, rz, Math.atan2(b.R[6], b.R[8])); // hp untouched — wounds keep
  m._fGrace = world.t + MECH_GRACE_S;
}
function mechGoal(world, grid, b, dt, fwdDir, opts) {
  const m = b.mechRef;
  if (!m) return;
  if (mechFallen(m)) { mechFallenTend(world, b); return; }
  if (world.t < (m._fGrace || 0)) { mechCommand(m, { travel: 0, lateral: 0 }); return; }
  const blockers = b.tracks !== "free" ? armorBlockers(world, b) : null;
  if (blockers) { // THE TRACKS LAW: own men in the lane stop the walk; they are told to step aside (the yield, verbatim reuse)
    mechCommand(m, { travel: 0, lateral: 0 });
    const fx = b.R[6], fz = b.R[8], fl = Math.hypot(fx, fz) || 1;
    for (const u of blockers) {
      if (u._yield && u._yield.until > world.t) continue;
      if (u.riding || u.pinned || u._fuse != null) continue;
      const side = ((u.pos.x - b.pos.x) * fz - (u.pos.z - b.pos.z) * fx) / fl >= 0 ? 1 : -1;
      const px = (fz / fl) * side, pz = (-fx / fl) * side;
      const p = clearSlot(world, u.pos.x + px * YIELD_M, u.pos.z + pz * YIELD_M, (u.hx || 0.28) + 0.35);
      if (!u._yieldHome) u._yieldHome = { x: u.pos.x, z: u.pos.z };
      u._yield = { x: p.x, z: p.z, until: world.t + YIELD_S };
    }
    return;
  }
  const order = b.order || "defend";
  if (order === "defend" || !b.dest) { mechCommand(m, { travel: 0, lateral: 0 }); return; }
  // MOVE/PATROL/ESCORT route legs — armorGoal's own bookkeeping shape on the hull
  if (order === "escort") {
    const sq = opts && opts.squads ? opts.squads.find((q) => q.id === b.escortId) : null;
    if (!sq) { b.order = "defend"; return; }
    const dx = sq.anchor.x - b.pos.x, dz = sq.anchor.z - b.pos.z, d = Math.hypot(dx, dz) || 1;
    if (d <= ARMOR_ESCORT_BACK + 3) { mechCommand(m, { travel: 0, lateral: 0 }); return; }
    b.dest = { x: sq.anchor.x - (dx / d) * ARMOR_ESCORT_BACK, z: sq.anchor.z - (dz / d) * ARMOR_ESCORT_BACK };
  }
  const destChanged = !b._routeDest || Math.hypot(b._routeDest.x - b.dest.x, b._routeDest.z - b.dest.z) > 0.5;
  if (destChanged || !b._route) {
    const r = planRoute(grid, b.pos.x, b.pos.z, b.dest.x, b.dest.z, { hull: true, team: b.team });
    if (r && !r.reached && r.pts.length) { const end = r.pts[r.pts.length - 1]; b.dest = { x: end.x, z: end.z }; } // the honest clamp
    b._route = r && r.pts.length ? r.pts : null;
    b._routeDest = { x: b.dest.x, z: b.dest.z };
  }
  while (b._route && b._route.length && Math.hypot(b._route[0].x - b.pos.x, b._route[0].z - b.pos.z) < MECH_ARRIVE) b._route.shift();
  if (Math.hypot(b.dest.x - b.pos.x, b.dest.z - b.pos.z) <= MECH_ARRIVE) {
    if (b.order === "patrol" && b._patA && b._patB) {
      const goingToB = Math.hypot(b.dest.x - b._patB.x, b.dest.z - b._patB.z) < 0.5;
      b.dest = goingToB ? { x: b._patA.x, z: b._patA.z } : { x: b._patB.x, z: b._patB.z };
      b._route = null; b._routeDest = null;
    } else { b.order = "defend"; b.dest = null; mechCommand(m, { travel: 0, lateral: 0 }); return; }
  }
  const wp = b._route && b._route.length ? b._route[0] : b.dest;
  const want = Math.atan2(wp.x - b.pos.x, wp.z - b.pos.z);
  if (b._mHead == null) b._mHead = m.state.heading;
  let herr = want - b._mHead;
  while (herr > Math.PI) herr -= 2 * Math.PI;
  while (herr < -Math.PI) herr += 2 * Math.PI;
  b._mHead += Math.max(-0.45 * dt, Math.min(0.45 * dt, herr));
  const turning = Math.abs(herr) > 0.5;
  mechCommand(m, { travel: turning ? 0.35 : 0.9, heading: b._mHead }); // the boss numbers // provisional (F5)
}
function mechGuns(world, b, dt, T, toUV) {
  const m = b.mechRef;
  if (!m || mechFallen(m)) return;
  const muzzle = { x: b.pos.x, y: b.pos.y + 2.0, z: b.pos.z };
  let tgt = armorScanFoes(world, b, muzzle, MECH_GUN, false, T, toUV);
  let struct = false;
  if (!tgt) { tgt = armorScanStructs(world, b, muzzle, MECH_GUN, T, toUV); struct = !!tgt; }
  if (!tgt) return;
  const d = Math.hypot(tgt.pos.x - b.pos.x, tgt.pos.z - b.pos.z);
  m.aimYaw = Math.atan2(tgt.pos.x - b.pos.x, tgt.pos.z - b.pos.z);
  m.aimRange = d;
  const torso = m.waist ? m.waist.b : m.hull;
  let bear = m.aimYaw - Math.atan2(torso.R[6], torso.R[8]);
  while (bear > Math.PI) bear -= 2 * Math.PI;
  while (bear < -Math.PI) bear += 2 * Math.PI;
  if (Math.abs(bear) > 0.12) return; // the waist is still turning
  mechFire(world, m); // rate-limited inside
  if (d > 14) mechMissiles(world, m);
  // the barrage answers structures and packed ground // provisional (F5)
  if (struct) { mechBarrage(world, m); return; }
  const pool = world._L ? (b.team === 1 ? world._L.foes : world._L.friends) : world.bodies;
  let near = 0;
  for (const e of pool) {
    if ((e.kind !== "unit" && e.kind !== "vehicle" && e.kind !== "mech") || !e.alive) continue;
    if (Math.hypot(e.pos.x - tgt.pos.x, e.pos.z - tgt.pos.z) < 8) near++;
  }
  if (near >= 4) mechBarrage(world, m);
}
DRIVERS.mech = { goal: mechGoal, guns: mechGuns };
```

**Step F3 — the machine dies detonating.** `src/depot/DepotGame.jsx` `stepDepot`, after the dead-structure sweep: a mech whose hull is dead detonates (the boss's end), releases its island, and leaves a wreck.

```js
// A DEAD MECH: one last magazine detonation, then the island lets go and
// the pieces settle where they stood — the wreck is the trophy.
if (world.mechs && world.mechs.length) for (let mi2 = world.mechs.length - 1; mi2 >= 0; mi2--) {
  const m = world.mechs[mi2];
  if (m.hull.alive) continue;
  const h = m.hull.pos;
  for (let i = 0; i < 3; i++) explode(world, h.x + (i - 1) * 1.5, h.y + i * 0.8, h.z, { r: 4.5, kv: 12, dmg: 20, crater: 0.8, attacker: m.team === 2 ? "player" : "enemy" });
  if (S.possess && S.possess.kind === "mech" && S.possess.id === m.hull.id) S.releasePossession();
  for (const L2 of m.links) { L2.mechRef = null; L2.team = 0; }
  world.mechs.splice(mi2, 1);
}
```

(`explode` joins this file's imports from core.js. The block is four statements: detonate, release possession, strip refs, splice.)

**Step F4 — asserts.** Checks M16–M21: a mech ordered MOVE walks its route legs and arrives to defend; a blocked lane brakes it and yields the men (tracks careful) and does not brake under tracks free; a knocked-down mech stands within the window with hp unchanged (the fall never wounds); shells landing while it is down DO wound; the driver's guns kill a seen conscript; a dead hull detonates, leaves `world.mechs` and leaves loose pieces.

## PHASE G — the enemy's machine, the market, and the cards

**Step G1 — `parkMech`.** `src/depot/muster.js`, after `parkTower` — the armor parking law at mech scale (vetted flat, cleared ring, a way out, fail-proof brute sweep):

```js
// THE MECH PARKS (owner, 2026-08-20): the armor law at crown scale.
const MECH_SPREAD = { hx: 2.2, hy: 1, hz: 2.2 }; // footprint vet // provisional (F5)
export function parkMech(world, grid, field, depotT, team) {
  if (!depotT) return null;
  const place = (bx, bz) => {
    const m = buildMech(world, { x: bx, z: bz, yaw: Math.atan2(-bx, -bz), team, hp: MECH.hp });
    m.thrustersOn = true; m.thrustAssist = true;
    m.hull.drv = "mech"; m.hull.order = "defend"; m.hull.tracks = "careful";
    m.hull.maxHp = MECH.hp; m.hull.homeX = bx; m.hull.homeZ = bz;
    if (team === 2) m.hull.bounty = MECH.bounty;
    return m.hull;
  };
  const clearAt = (bx, bz) => {
    const cell = grid.cellAt(bx, bz);
    if (!cell || cell.blocked || cell.ice || cell.water || cell.wallId || cell.steep) return false;
    if (Math.hypot(bx - OBJ_POS.x, bz - OBJ_POS.z) < 5) return false;
    if (slotBlockedPublic(world, bx, bz, 4.5)) return false; // provisional (F5)
    if (world.bodies.some((o) => (o.kind === "vehicle" || o.kind === "mech") && o.alive && Math.hypot(o.pos.x - bx, o.pos.z - bz) < 8)) return false;
    return true;
  };
  for (let rr = 16; rr <= 32; rr += 1.5) for (let k = 0; k < 16; k++) {
    const az = (k / 16) * Math.PI * 2 + 0.35;
    const bx = depotT.x + Math.sin(az) * rr, bz = depotT.z + Math.cos(az) * rr;
    if (clearAt(bx, bz) && armorSpread(field, bx, bz, MECH_SPREAD) < 0.28 && planRoute(grid, bx, bz, 0, 0, { hull: true, team })) return place(bx, bz);
  }
  let flat = null, flatSp = 1e9; // fail-proof: the flattest clear cell parks it anyway
  for (let gz = 0; gz < GRID_H; gz++) for (let gx = 0; gx < GRID_W; gx++) {
    const wp = grid.gridToWorld(gx, gz);
    const d = Math.hypot(wp.x - depotT.x, wp.z - depotT.z);
    if (d > 36 || d < 14 || !clearAt(wp.x, wp.z)) continue;
    const sp = armorSpread(field, wp.x, wp.z, MECH_SPREAD);
    if (sp < flatSp) { flatSp = sp; flat = wp; }
  }
  return flat ? place(flat.x, flat.z) : null;
}
```

(`buildMech` and `MECH` join muster.js imports.)

**Step G2 — the pool and the mirror.** `src/depot/muster.js`: `PICK_POOL` gains `{ key: "hero_mech", kind: "mech" }` (after `hero_apc`); `mirrorFieldKey` gains, before the tower branch: `if (pick.kind === "mech") { parkMech(world, S, depotE, grid, field ...` — exact form: `if (pick.kind === "mech") { parkMech(world, grid, field, depotE, 2); return; }`

**Step G3 — its replacement walk.** `src/depot/bell.js`, the hero-tier block (~:131): after the APC clause, `else if (depotE4 && !(world.mechs || []).some((m) => m.team === 2 && m.hull.alive) && open("hero_mech") && S.reg.scrap >= heroPrice("hero_mech")) { S.reg.scrap -= heroPrice("hero_mech"); parkMech(world, grid, field, depotE4, 2); }` — and `heroPrice`'s fallback map gains `hero_mech: MECH.cost` (`MECH` joins bell.js imports; `parkMech` joins the muster import line). Draw-free, like both hulls — zero draw movement.

**Step G4 — the market.** `src/depot/market.js`: `MARKET_K` gains `heroMech: 1, // owner: one standing machine walls the second — both sides' counted together // provisional (F5)`; `marketCounts` gains `else if (b.kind === "mech" && b.alive) add("heroMech", 1);`; `computePrices` gains `player.hero_mech = priced(MECH.cost, "heroMech", counts);` and `foe.hero_mech = priced(MECH.cost, "heroMech", counts);` (`MECH` joins the import).

**Step G5 — the bar, the card, the portrait.** 

- `src/depot/DepotGame.jsx` `PALETTE`: `{ key: "hero_mech", label: "MECH", icon: "✇", cost: MECH.cost }` after the APC row (`MECH` joins the specs import). `setMode`'s hero branch: `m === "hero_bison" || m === "hero_apc"` → add `|| m === "hero_mech"`; `S.buyHero`'s kind line handles the third key: `const kind = key === "hero_apc" ? "apc" : key === "hero_mech" ? "mech" : "bison";` and its parking call routes `kind === "mech" ? parkMech(world, grid, field, depotP, 1) : parkArmor(...)` (`parkMech` joins the muster import line). The hero two-tap arm, price toast, and pacing hold verbatim.
- `placePick` and `placeHire` gain a `pick.kind === "mech"` branch beside the hull branch: vet `armorSpread(field, wp.x, wp.z, MECH_SPREAD) < 0.28` (import `MECH_SPREAD` alongside `parkMech`) and `slotBlockedPublic(world, wp.x, wp.z, 4.5)`, then `buildMech` team 1 with the Step G1 hull fields, facing the valley.
- `src/depot/infocards.js` `CARDS` gains: `hero_mech: { label: "MECH", role: "The crown machine. A walking siege engine — cannon, rocket salvo, and a saturation barrage; men die under its feet. Slow, dear, and answered only by another.", n: null, hp: MECH.hp, dmg: null, range: null, speed: null, skills: ["DEFEND", "MOVE", "PATROL", "ESCORT", "TRACKS SAFETY", "TAKE CONTROL"] }` (`MECH` joins the import). Copy is served for the owner's approval with this plan.
- `src/render/portrait.js` `buildPortraitModel`: `if (key === "hero_mech") return buildPortraitMech();` and a ~14-line `buildPortraitMech()` — grey toon boxes at the rig's proportions (hull slab, torso slab, two legs of three boxes, head, pod) — the boxes-first look in miniature.

**Step G6 — the manual learns the mech.** `src/ui/FieldManual.jsx`: one new card after YOUR ARMOR — title **THE MECH**, copy: *"The rarest card in the deck is a walking siege engine. Both sides can field one. It is slow — watch it come, and answer it with shells, mines, or its twin. Take control to drive it yourself; its feet are a weapon."* — and `MANUAL_REV` bumps 5 → 6 (the tour returns; the two MANUAL_REV pins re-teach under the license). Copy is served for the owner's approval with this plan.

**Step G7 — asserts.** Checks M22–M26: the pool and hand are eighteen with `hero_mech` in every seat; `parkMech` on the fixture map stands a team-2 mech inside 36 m of the depot with drv/order/tracks/bounty set; `mirrorFieldKey("hero_mech")` fields it; the bell's replacement walk re-parks a dead team-2 mech when the plan is owned and the books cover the price (draw count unmoved — the draw-law loop pin holds); `computePrices` carries `hero_mech` on both tables and one standing machine doubles it.

## PHASE H — possession, phone and desktop

**Step H1 — the mech is tappable and takes the pie.** `src/depot/DepotGame.jsx` `tapAt`'s candidate scan: the vehicle branch condition `b.kind === "vehicle"` → `(b.kind === "vehicle" || b.kind === "mech")` (same `TAP_HULL_M` radius, same `"veh:"` key — the whole selection/pie machinery follows for free). `hud.vehRadial`'s builder: label by kind — `v.kind === "mech" ? "MECH"` — and the APC-only slots stay APC-gated by the existing `vtype` checks. `S.orderVehicle`, `S.toggleTracks`, `consumeVehOrderTap`, the patrol-line accept: verify each reads the body generically (they do — byId + fields); no edit expected, verified at dispatch.

**Step H2 — TAKE CONTROL forks by kind.** `S.takeControlVehicle`: if the selected body is a mech, possess as its own kind — `S.possess = { kind: "mech", id: v.id };` with the same hygiene lines (order defend, clear selections); no reticle is seeded (the mech aims by torso and range, the range's own convention).

**Step H3 — the possessed-mech frame block.** The frame loop, beside the possessed-vehicle block: camera locks to the hull; commands feed INSIDE the sim catch-up loop (the range's cadence law — per-frame feeds fall the machine). Port `MechRange.jsx`'s `feedCommands` verbatim-minus-jets into a `feedMechCommands(m, cdt)` closure: left stick / WASD screen-relative walk (`tf` capped 0.6/−0.42, `tl` ±0.22), right stick / A,D turn with the 0.5 rad steering lock, hard-over 0.6 s → `mechPivot`, chassis-follow while moving, `mech.aimRange` from the range slider (touch) or mouse distance (desktop), `mech.aimYaw` from the ◀ ▶ trim (touch, waist-clamped) or mouse bearing (desktop, the screen-offset math — the camera is hull-locked so screen center IS the mech). Triggers, at most one attempt per sim tick, each SIGHT-GATED at the aim point before the engine call (the possessed-fire law — `fieldReaches(T, invW(aimPoint), 1)` with the aim point from `mechAimDir`): FIRE held → `mechFire`; MSL → `mechMissiles`; BRG → `mechBarrage`; PUNT → `mechPunt`; 180 → `mechAboutFace`. Desktop keys: click fires, V missiles, B barrage, C punt, T 180 (registered in the war's own key handlers, active only while a mech is possessed).

**Step H4 — the buttons.** Touch, shown only while `hud.possessed.kind === "mech"`: the existing left stick (walk) and right stick (relabeled TURN for mechs); the FIRE button (shared); three new `P.btnBig` buttons — `data-mech-msl` ▲▲ MSL (cooldown-grey off `mech._lastMsl`), `data-mech-brg` ▲▲▲ BRG (off `_lastBar`), `data-mech-punt` PUNT — stacked above FIRE; the range slider + ◀ ▶ trim pair, the range screen's own DOM ported with depot styling; RELEASE unchanged. `hud.possessed` gains `kind: "mech"` from the hud builder (byId branch mirroring the vehicle one, label "MECH"). Desktop needs no new DOM (keys + mouse); the hint line in the possessed chip names the bindings.

**Step H5 — release, death, save.** `S.releasePossession` gains the mech branch: back to `depotDrive`-less auto (`b.order = "defend"`, dest cleared — the driver resumes next tick). Death release is Phase F. A bell save mid-possession never records it (`serializeFront` reads no possession state — the standing law, verified by the existing pin).

**Step H6 — the trajectory preview.** While a mech is possessed, the range's low-passed ballistic preview draws through the war renderer's own `R.setTraj` (the code ported from `MechRange.jsx` :418-441); cleared on release. Render-only.

**Step H7 — asserts.** Checks M27–M29 (mechanics only, feel is the owner's): `takeControlVehicle` on a mech sets `possess.kind "mech"` and release returns it to defend; the possessed sight gate refuses a fire at unseen ground (headless: `fieldReaches` false → `mechFire` not called — cooldown timestamp unmoved); the possessed skip in `stepDrivers` leaves a possessed mech's driver silent.

## PHASE I — the renderer holds two machines

**Step I1 — the budget.** `src/render/renderer.js` (~:898): the mech pool `40` → `96`; the draw-loop cap `mi >= 32` → `mi >= 88`; the pod guard `mi < 23` → `mi < 84`; the thruster guard `mi < 40` → `mi < 96`.

**Step I2 — per-mech hardware.** The pod/thruster block currently keys off the single `torsoB` found in the piece sweep. Rewrap: collect `const torsos = [];` in the sweep (`if (b.visTag === "torso") torsos.push(b);`), then run the existing pod + thruster drawing once per entry (`for (const torsoB of torsos) { …existing block, indented… }`). Body of the block verbatim; only the wrapper is new.

**Step I3 — assert.** Check M30: with two mechs built, the instance count written is ≥ 40 and both torsos carry a pod (exported count via `chunkStats`-style debug or a pure count of `visTag` torsos — headless-checkable form chosen at dispatch and named in the report).

## PHASE J — the suite, the gates, the landing

**Step J1 — the new era file.** `scripts/tests/12-the-mech.mjs`, registered in `scripts/depot-test.mjs` after era 11 (`await import("./tests/12-the-mech.mjs");` before `finish()`). Thirty checks, M1–M30 as named in Phases B–I, written failing-first where the step's code follows them. Fixture worlds use `makeWorld` + a flat field (the era-07 armor idioms); seeds drawn fresh at 140+ and named in the report.

**Step J2 — the sweep ledger (the license, pre-named).** The type count moves seventeen → eighteen. Six literal pins re-teach, content identical, each reported old → new:

| Site | Pin |
|---|---|
| `scripts/tests/07-armor-demolition.mjs:873` | "seventeen" / `=== 17` → eighteen / 18 |
| `scripts/tests/10-command-refit.mjs:276` | `PICK_POOL.length === 17` (×2) → 18 |
| `scripts/tests/11-hiring-hall.mjs:59-60` | `HAND_KEYS.length === 17` (+ set size) → 18 |
| `scripts/tests/11-hiring-hall.mjs:154-156` | pool "seventeen" / 17 → eighteen / 18 |
| `scripts/tests/11-hiring-hall.mjs:457-458` | 17/17 → 18/18 |
| `scripts/tests/11-hiring-hall.mjs:559-560` | 17/17 → 18/18 |

**Value-shift license (the seed-91 precedent):** fixed-seed boot-deal and draft fixtures (era 10/11, seeds 91, 92, 94, 130, 131 and any sibling the run surfaces) may re-base their DEALT VALUES to the eighteen-key pool — draw counts, distinctness, and every structural assert hold unmoved; each re-base reported old → new. Boot draws stay exactly 15; the bell's draw law loop stays exactly as pinned. A test failing any other way STOPS the task.

**Step J3 — the gates (run ONLY these).** `node scripts/depot-test.mjs` — expected 1618 → 1648/0 (thirty new checks; the ledger's re-teaches are count-neutral); `node scripts/golden.mjs` — 7/7, untouched by construction; `node scripts/depot-lint.mjs` — clean (no `Math.random`, none introduced; mech.js draws nothing); the keystone hash and draw count — byte-identical (the keystone fixture fields no mech); smoke — the standing smoke run, green, plus its boot proving the eighteen-key draft screen mounts.

**PROBE VERDICT — ACCEPTED OVER THE LINE (owner's override, at landing):** the two-mech duel amid the full field measured worst windows of 13.25 ms and 13.47 ms against the 11.0 line, with repeat noise of 10–25% on a machine shared with two preview servers and the orchestrator session. The owner accepted the number knowingly and ordered the landing; the two-mech performance work (the walker island is the suspect) is deferred to its own later task. Every other gate was green: suite 1648/0, golden 7/7, lint clean, keystone byte-identical, smoke green. The agent's dozen license-extension re-teaches and three named deviations stand ratified with the landing.

**Step J4 — the probe runs here** (Phase A's instrument, after Phase F, before Phase G interface work merges into the landing): two repeats on the Pi, numbers into the report. Over the line = stop and report.

**Step J5 — the landing.** `src/version.js` → mk1.92; build AFTER the bump; commit; push. The report opens with the probe's numbers, names every fixture seed, every re-teach and re-base old → new, and every deviation as its own labeled bullet.

**The owner's live check** (feel, look, and sound are his alone): draft or buy a mech; watch it walk the valley; brake it against its own men and take the safety off; take control — walk, turn, cannon, salvo, barrage, kick, 180, on phone and desktop; duel the enemy's machine; watch a knockdown and the stand; save mid-war with one standing and resume it.

## Named exclusions

- The art — its own later work (boxes ship).
- The hip-yaw gait branch — its own decision, untouched.
- Commander doctrine deeper than the armor's (mech-specific caution/boldness) — Enemy Front.
- Mech sound identity — the soundboard's process, its own later work; the engine's existing shell/rocket voices carry it meanwhile.
